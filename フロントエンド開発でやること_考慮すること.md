# フロントエンド開発でやること・考慮すること(レベル1〜10)

対象読者: PO/SCM/エンジューアを問わず、フロントエンド開発に関わる人向け。工程(要件定義→実装→リリース)の話ではなく、**「どこまで考慮できるか」の成熟度**をレベル1〜10で整理しました。レベルが上がるほど、下のレベルでやっていることに加えて、より広い範囲を考慮できるようになります(後のレベルは前のレベルを内包する、積み上げ式です)。例は`front-end/`のサンプルプロジェクトから取っています。

関連ドキュメント: チケットを任せるエンジニアのレベル分けは [フロントエンドのタスクの粒度.md](フロントエンドのタスクの粒度.md) の「追記2」を参照してください。あちらは「このレベルの人にどのチケットを渡すか」、本ドキュメントは「そのレベルとは具体的に何を考慮できることを指すのか」を掘り下げたものです。

---

## Level 1: 見た目を再現できる

**やること**: デザイン通りにHTML/CSSでレイアウトを組む。静的な画面を作る。

**考慮すること**: デザインカンプと見た目が一致しているか(余白・文字サイズ・色)。

---

## Level 2: ユーザー操作に反応する画面が作れる

**やること**: `useState`やイベントハンドラ(`onClick`等)を使い、クリックや入力に応じて画面が変わる部分を実装する。例: [`TicketForm`](front-end/src/components/TicketForm.tsx)の入力欄。

**考慮すること**: Level 1に加えて、「操作した通りに画面が反応しているか」。入力値が正しく保持されているか。

---

## Level 3: バックエンドと通信できる

**やること**: `fetch`でAPIからデータを取得・送信する。ローディング中・成功・失敗の基本的な出し分けを実装する。例: [`src/api/client.ts`](front-end/src/api/client.ts)、各ページの`useEffect`。

**考慮すること**: Level 1・2に加えて、「通信が終わるまでの間、画面はどう見えるべきか」「通信が失敗したら何を表示するか」。

---

## Level 4: 複数画面をまたいだ一連の流れが作れる

**やること**: 画面遷移・URL設計を行い、複数ページにまたがる操作(一覧→詳細→編集、など)を実装する。例: [チケット一覧→詳細→編集](front-end/src/pages/detail/App.tsx)の一連の流れ。

**考慮すること**: Level 1〜3に加えて、「画面間でどのデータをどう受け渡すか」(例: URLのIDから詳細情報を取得する)、「遷移前後で状態が壊れないか」。

### 必要になるReactの知識

- **URLとコンポーネントの対応付け**: SPAなら`react-router-dom`(`Routes`/`Route`/`useParams`/`useNavigate`)、本サンプルのようなMPAなら「URL(パス)自体からIDを読み取る」自前の仕組み。どちらも「今表示すべき画面・データをURLからどう決めるか」という同じ問題を解いている
- **マウント時のデータ取得**: `useEffect`の依存配列にURLパラメータ(`id`など)を入れておくと、同じコンポーネントのままIDだけ変わった場合にも再取得される
- **画面間のデータの渡し方の選択肢**: (a) URLパラメータ/クエリに乗せる、(b) 遷移前にサーバーへ保存してIDだけ渡す、(c) `sessionStorage`等に一時保存する、(d) SPAならグローバルなstate(Context等)に持つ。**サーバーに保存できるデータは(b)を優先する**(ページ再読み込みやブラウザの戻るで消えないため)のが基本方針
- **遷移のタイミング制御**: 「保存が成功したら遷移する」という順序を保証すること(保存前に遷移してしまうと、遷移先で存在しないデータを表示してしまう)

### 具体例

**このサンプル(MPA)でのID受け渡し**: URLの`/tickets/1`から、SPAルーターを使わずにIDを読み取っている。

```ts
// src/utils/path.ts
export function getTicketIdFromPath(): string | undefined {
  const parts = window.location.pathname.split("/").filter(Boolean);
  return parts[1]; // "/tickets/1" -> "1"
}
```

```tsx
// src/pages/detail/App.tsx
const id = getTicketIdFromPath();

useEffect(() => {
  if (!id) return;
  fetchTicket(id).then(setTicket); // URLのidをもとに、その都度サーバーから取得し直す
}, [id]);

async function handleSubmitOnEditPage(saved: Ticket) {
  // 保存が終わってから、次の画面へ実際に遷移する(フルリロード)
  window.location.href = `/tickets/${saved.id}`;
}
```

**もしSPA(react-router)で同じことをするなら**、考え方は同じでも書き方が変わる。

```tsx
import { useParams, useNavigate } from "react-router-dom";

function TicketDetailPage() {
  const { id } = useParams<{ id: string }>(); // URLからidを取得する部分がライブラリ任せになる
  const navigate = useNavigate();

  async function handleDelete() {
    await deleteTicket(id!);
    navigate("/"); // フルリロードせず、画面だけを差し替える
  }
}
```

**複数ステップの入力(例: 入力→確認→完了)でデータを引き継ぎたい場合**は、`sessionStorage`のような一時領域を使う手もある(機密情報は入れない前提)。

```tsx
// 入力ページ: 確認ページに渡す前に一時保存してから遷移する
function handleNext(input: TicketInput) {
  sessionStorage.setItem("ticketDraft", JSON.stringify(input));
  window.location.href = "/tickets/new/confirm";
}

// 確認ページ: 一時保存したデータを読み出して表示する
const draft: TicketInput | null = JSON.parse(
  sessionStorage.getItem("ticketDraft") ?? "null"
);
```

---

## Level 5: 再利用性・保守性を考慮した設計ができる

**やること**: 共通化できる部分をコンポーネント・関数に切り出す。責務ごとにファイルを分割する。例: 新規作成・編集で共用する[`TicketForm`](front-end/src/components/TicketForm.tsx)、汎用[`Modal`](front-end/src/components/Modal.tsx)。

**考慮すること**: Level 1〜4に加えて、「今書いているコードは、後で自分以外の人が読んでも理解できるか」「似た画面が増えたときに、コピペではなく共通化できる作りになっているか」。

---

## Level 6: エラー・エッジケースを網羅できる

**やること**: 通信失敗、0件、権限エラー、不正な入力(例: [zip内にCSVが無い場合](front-end/src/utils/parseZipCsv.ts))など、正常系以外のケースを洗い出して実装する。連打防止(二重送信対策)を入れる。

**考慮すること**: Level 1〜5に加えて、「ユーザーが想定外の操作をしたときに画面が壊れないか」「エラーメッセージはユーザーが次に何をすればいいか分かる内容か」。

---

## Level 7: 非機能要件(性能・アクセシビリティ・セキュリティ)を考慮できる

**やること**: 表示速度・大量データ時の挙動を確認する。キーボード操作やスクリーンリーダーへの配慮(`aria-label`等)を入れる。ユーザー入力やアップロードファイルを信頼しすぎない実装にする。

**考慮すること**: Level 1〜6に加えて、「動く」だけでなく「誰にとっても快適で安全に使えるか」。この観点はチケットの完了条件に明記されないことが多く、自発的に気づけるかどうかがこのレベルの分かれ目になる。

### 性能(パフォーマンス)

**必要な知識**
- Reactの再レンダリングの仕組み(stateが変わるとコンポーネントが再実行される、なぜ不要な再レンダリングが起きるか)
- `useMemo`/`useCallback`によるメモ化(重い計算・関数の再生成を防ぐ)、およびメモ化しすぎても逆に読みにくくなるという判断
- リストレンダリング時の`key`の付け方(indexではなくID等の安定した値を使う理由)
- 頻繁に発生するイベント(入力・スクロール)の間引き(デバウンス/スロットル)
- 画像・ファイルサイズ、通信回数の最適化

**具体例**: 検索ワード入力のたびにAPIを呼ぶと通信が増えすぎるため、入力が止まってから呼び出す(デバウンス)。

```tsx
const [keyword, setKeyword] = useState("");

useEffect(() => {
  if (!keyword) return;
  const timer = setTimeout(() => {
    fetchTickets({ keyword }).then(setTickets);
  }, 300); // 300ms入力が止まってから検索する
  return () => clearTimeout(timer); // 入力のたびに前回のタイマーは解除する
}, [keyword]);
```

一覧の集計のような重い計算は、依存する値が変わったときだけ再計算する。

```tsx
const totalPoints = useMemo(
  () => tickets.reduce((sum, t) => sum + t.points, 0),
  [tickets] // ticketsが変わらない限り再計算しない
);
```

(このサンプルの[`TicketListPage`](front-end/src/pages/list/App.tsx)では、`key={ticket.id}`のようにIDをkeyに使っている。これがindexだと、一覧の並び替えや削除のたびに関係ない行まで再描画される)

### アクセシビリティ(a11y)

**必要な知識**
- セマンティックHTML(クリック可能な要素は`<div onClick>`ではなく`<button>`を使う。フォームは`<label>`と`<input>`を紐付ける)
- `aria-*`属性の役割(`aria-label`で読み上げ用の名前を補う、`role="alert"`で動的なメッセージを即座に読み上げさせる)
- キーボードだけで操作できるか(Tabで要素に到達できるか、モーダルをEscで閉じられるか)
- 色だけに意味を持たせない(色覚特性によっては伝わらない)

**具体例**: エラーメッセージを、画面が見えていなくても(スクリーンリーダーで)即座に伝わるようにする。

```tsx
{error && (
  <p role="alert" style={{ color: "red" }}>{error}</p>
)}
```

モーダルをキーボード(Esc)でも閉じられるようにする。

```tsx
// src/components/Modal.tsx に追記するイメージ
useEffect(() => {
  if (!isOpen) return;
  function handleKeyDown(e: KeyboardEvent) {
    if (e.key === "Escape") onClose();
  }
  document.addEventListener("keydown", handleKeyDown);
  return () => document.removeEventListener("keydown", handleKeyDown);
}, [isOpen, onClose]);
```

### セキュリティ

**必要な知識**
- XSS(クロスサイトスクリプティング): ユーザーが入力した文字列を、そのままHTMLとして描画しないこと
- ユーザー入力・アップロードファイルは信頼しない(サイズ、拡張子、中身の形式チェックはフロントでも行うが、最終的な保証はバックエンドで行う前提を理解しておく)
- APIキーや秘密情報をフロントエンドのコードに埋め込まない(フロントのコードは誰でも読める)
- 依存ライブラリの脆弱性(`npm audit`等で定期的に確認する)

**具体例**: Reactは通常のテキスト描画では自動でエスケープしてくれるため、基本的にはそのまま書けばXSSを防げる。危険なのは`dangerouslySetInnerHTML`を使うとき。

```tsx
// OK: Reactが自動でエスケープしてくれるので、<script>のような文字列が入っても実行されない
<p>{comment.body}</p>

// NG: HTMLとしてそのまま描画してしまうため、ユーザー入力にスクリプトが混入すると実行されうる
<p dangerouslySetInnerHTML={{ __html: comment.body }} />
```

アップロードするファイルを、拡張子・サイズの時点でフロント側でも軽くチェックしておく(サンプルの[CSV集計画面](front-end/src/pages/upload/App.tsx)を拡張する例)。

```tsx
function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {
  const file = e.target.files?.[0] ?? null;
  if (file && !file.name.toLowerCase().endsWith(".zip")) {
    setError("zipファイルを選択してください");
    return;
  }
  if (file && file.size > 10 * 1024 * 1024) {
    setError("ファイルサイズが大きすぎます(10MBまで)");
    return;
  }
  setSelectedFile(file);
}
```

---

## Level 8: チーム全体で使えるパターン・設計を主導できる

**やること**: 他メンバーが真似できる型(コンポーネント分割の指針、API呼び出しパターン、状態管理の指針)を最初に作る。他メンバーのPRをレビューし、設計の一貫性を保つ。例: [チケット詳細画面](front-end/src/pages/detail/App.tsx)で確立したパターンを、後続の新規作成・編集画面が踏襲している。

**考慮すること**: Level 1〜7に加えて、「自分が書いたコードは、他のメンバーが同じパターンで書き進められる状態になっているか」「チーム全体のコードの一貫性・学習コストはどうか」。

---

## Level 9: ビジネス・プロダクト全体への影響を考慮できる

**やること**: この機能がビジネス指標(コンバージョン、GA4での計測など)にどう影響するかを踏まえて実装方針を決める。優先順位や工数とのバランスを判断する。例: [GA4計測のためにSPAではなくMPA構成を選んだ判断](front-end/README.md)。

**考慮すること**: Level 1〜8に加えて、「この画面・機能は事業のどの数字に効くのか」「今のリソースでやるべきことはこれで合っているか」。ここからはエンジニアだけでなくPO/SCMの視点そのものでもある。

---

## Level 10: 組織・中長期の技術戦略を考慮できる

**やること**: 技術選定・アーキテクチャの意思決定を行う。数年単位での保守コスト・拡張性・採用/育成のしやすさまで見据えて方針を決める。例: 「今後フロントエンドメンバーが増えても迷わないように、ページ構成・命名規則・チケットの切り方の型を最初に作る」といった意思決定。

**考慮すること**: Level 1〜9に加えて、「この技術選定は3年後も破綻しないか」「チームの人数や経験構成が変わっても回る仕組みになっているか」「フロントエンドの意思決定が、採用・育成・組織構造にどう跳ね返るか」。

---

## まとめ

| Level | このレベルで新たに考慮できるようになること |
|---|---|
| 1 | デザイン通りに再現できているか |
| 2 | 操作に対して画面が正しく反応しているか |
| 3 | 通信中・成功・失敗をどう見せるか |
| 4 | 画面間のデータの受け渡し・状態の一貫性 |
| 5 | 再利用性・保守性(他の人が読める/使えるコードか) |
| 6 | エラー・エッジケースの網羅 |
| 7 | 性能・アクセシビリティ・セキュリティ |
| 8 | チーム全体のコードの一貫性・型づくり |
| 9 | ビジネス指標・優先順位への影響 |
| 10 | 組織・中長期の技術戦略 |

**使い方の目安**: 個人の育成計画(「今どのレベルにいて、次に何を意識すればいいか」)や、[フロントエンドのタスクの粒度.md](フロントエンドのタスクの粒度.md)の「Lv.E1〜E5」とあわせて、誰にどこまでの判断を任せられるかの目線合わせに使えます。Level 7までは主にエンジニア個人のスキル、Level 8以降はチーム・組織を見る視点(テックリードやPO/SCMと重なる領域)という区切りで捉えると分かりやすいです。
