# フロントエンドのタスクの粒度(5段階)

対象読者: SCM/POとしてフロントエンドメンバーにチケットを振る人向け。「このチケット、粒度として適切なのか大きすぎる/小さすぎるのか」を判断するための物差しをまとめました。具体例は、これまで一緒に作った`front-end/`のサンプルプロジェクト(チケット管理アプリ+CSV集計機能)から取っています。

関連ドキュメント: チケットの切り方(縦切り/横切り)は [definition-of-done.md](definition-of-done.md)、実際のチケット文面例は [フロントエンドタスクの例.md](フロントエンドタスクの例.md) を参照してください。本ドキュメントはその手前にある「そもそもどのサイズでチケットを切るべきか」を整理したものです。

---

## 全体像

| 粒度 | 呼び方 | 目安の作業時間 | 1チケットとして適切か | 例 |
|---|---|---|---|---|
| Lv.1 | 関数/フックレベル | 数十分〜1時間 | ✕ 小さすぎる(単体では立てない) | 日付フォーマット関数を作る |
| Lv.2 | コンポーネントレベル | 1〜3時間 | △ 単体だと小さいが、共通部品化の文脈ではあり | モーダルコンポーネントを作る |
| Lv.3 | 画面(ページ)レベル | 半日〜2日 | ◯ 最も一般的なチケットの粒度 | チケット詳細画面を実装する |
| Lv.4 | 機能(フィーチャー)レベル | 2〜5日 | ◯ 複数画面にまたがる一連の価値の単位 | チケットのCRUD機能一式を実装する |
| Lv.5 | エピック/プロダクトレベル | 数週間〜 | ✕ そのままは積まない(Lv.3/4に分割する) | チケット管理システムのフロントを新規構築する |

**基本方針**: スプリントに積むチケットはLv.3〜Lv.4に収める。Lv.1〜Lv.2は基本的にLv.3チケットの中の「作業手順の1ステップ」として現れるものであり、独立したチケットにはしない(例外は後述)。Lv.5はエピックとして持っておき、着手前にLv.3/4へ分割してからスプリントに積む。

---

## Lv.1: 関数/フックレベル

**説明**: 特定の計算・変換・判定だけを行う、純粋な関数やカスタムフック単体。画面には依存しない。

**目安時間**: 数十分〜1時間

**具体例**
- 金額をカンマ区切り文字列にフォーマットする関数を作る
- 日付を`YYYY-MM-DD`形式に変換する関数を作る
- サンプルプロジェクトでいう [`aggregatePointsFromZip`](front-end/src/utils/parseZipCsv.ts) のうち、「CSVの1行からC列の値を数値として取り出す」部分だけを切り出す、など

**チケットとして**: 基本的に単体でチケット化しない。「画面が動くか」という価値に直結しないため、これだけ完了しても誰も検証できません。Lv.3チケットの作業手順の1ステップとして現れます。

**例外**: 複数の画面・チームで共有するユーティリティ関数(社内共通ライブラリ化する等)を独立して整備する場合は、Lv.1でもチケット化されることがあります。

---

## Lv.2: コンポーネントレベル

**説明**: props(入力)を受け取って表示する、画面の一部品。状態やAPI通信を持たない「見た目だけ」のことが多い。

**目安時間**: 1〜3時間

**具体例**
- [`TicketStatusBadge`](front-end/src/components/TicketStatusBadge.tsx)(ステータスに応じて色・文言を出し分けるバッジ)を作る
- [`Modal`](front-end/src/components/Modal.tsx)(開閉状態を受け取って表示する汎用モーダル)を作る
- [`TicketForm`](front-end/src/components/TicketForm.tsx)(入力項目をまとめたフォーム部品)を作る

**チケットとして**: これも単体では小さすぎることが多く、通常はLv.3チケットの中の1ステップです。ただし、**デザインシステム/共通コンポーネント集を整備するプロジェクト**では、「Buttonコンポーネントを作る」のようにLv.2がそのままチケットになることがあります(この場合、完了条件は「Storybookに登録されている」「複数のバリエーション(primary/secondary等)に対応している」など)。

---

## Lv.3: 画面(ページ)レベル

**説明**: 1つのURL・1つの画面として、ユーザーが実際に触って完結する単位。表示・状態管理・API通信をひとまとめにする。**最も一般的なチケットの粒度**です。

**目安時間**: 半日〜2日

**具体例**
- チケット一覧画面([`/`](front-end/src/pages/list/App.tsx))を実装する
- チケット詳細画面([`/tickets/:id`](front-end/src/pages/detail/App.tsx))を実装する(ステータス変更・削除を含む)
- CSV集計画面([`/upload`](front-end/src/pages/upload/App.tsx))を実装する

**完了条件の書き方例**(チケット詳細画面の場合)
- URLにアクセスすると、対象チケットの情報が表示される
- ステータスを変更すると、APIが呼ばれ画面に反映される
- 削除すると、一覧画面に遷移する
- 存在しないIDの場合、エラー表示になる

**チケットとして**: [definition-of-done.md](definition-of-done.md)の「1チケット=1〜3日で終わる大きさ」にちょうど収まりやすく、スプリントの主力になる粒度です。作業手順は自然と「型定義→API関数→コンポーネント→ページへの組み込み→動作確認」という一連の流れになります(具体的な手順例は[フロントエンドタスクの例.md](フロントエンドタスクの例.md)参照)。

---

## Lv.4: 機能(フィーチャー)レベル

**説明**: 複数の画面にまたがる、ユーザーから見た「一連のできごと」の単位。Lv.3の画面チケットが複数集まったもの、と捉えてもよい。

**目安時間**: 2〜5日(5日を超えそうならLv.3単位に分割してスプリントを分ける)

**具体例**
- チケットのCRUD機能一式を実装する(一覧・詳細・新規作成・編集・削除の4〜5画面セット)
- CSV集計機能を追加する(アップロード画面の実装 + 集計ロジック + モーダル表示、[フロントエンドタスクの例.md](フロントエンドタスクの例.md)の「題材2」がこれに相当)
- チケットへのコメント機能を追加する(詳細画面の改修+コメントAPI連携、同ドキュメントの「題材1」)

**チケットとして**: 1つのエピックの中で「これができればユーザーに価値が届く」という最小単位。5日を超える見込みなら、SCM/POとしては無理に1枚にせず、Lv.3(画面単位)に分割してスプリント計画に載せる方が進捗が見えます。ただし分割する際も、[definition-of-done.md](definition-of-done.md)にある「縦切り(機能単位)」を保つこと(工程で横に切らない)。

---

## Lv.5: エピック/プロダクトレベル

**説明**: 複数の機能(Lv.4)を束ねた、プロジェクト・リリース単位の大きな塊。それ自体は「作業」ではなく「まとまり」。

**目安時間**: 数週間〜数ヶ月

**具体例**
- チケット管理システムのフロントエンドを新規構築する(一覧・詳細・作成・編集・CSV集計・認証など全部)
- 既存のSPA構成をやめて、GA4計測のためにMPA構成(画面ごとにHTML/フルリロード遷移)へ全面移行する
- チケット管理システムに「コメント機能」「添付ファイル機能」「通知機能」をまとめて追加する四半期の開発計画

**チケットとして**: Lv.5をそのままチケットにしてスプリントには積まない。**エピックとして持ち、着手前にLv.3〜Lv.4へ分割してから積む**。分割しきれていない(仕様が固まっていない)場合は、[definition-of-done.md](definition-of-done.md)の「Definition of Ready」にある通り、そのままスプリントに入れず「調査チケット」を先に切る。

---

## 実務での使い分けまとめ

```
Lv.5 エピック(数週間〜)
  └─ Lv.4 機能チケット(2〜5日) ← エピックをここまで分割してから着手可否を判断
       └─ Lv.3 画面チケット(半日〜2日) ← スプリントの主力。基本はこの粒度で積む
            └─ Lv.2 コンポーネント / Lv.1 関数 ← エンジニアが画面チケットの中で自然に作る単位。基本は独立チケット化しない
```

- **SCM/POが管理する単位**: Lv.4(機能)とLv.5(エピック)。バックログの整理・優先順位付けはここで行う
- **スプリントに積む単位**: Lv.3(画面)、まれにLv.4がそのまま収まるなら4のままでもよい
- **エンジニアが個人の裁量で刻む単位**: Lv.1〜Lv.2。チケットの説明欄や作業手順に書かれていれば十分で、別チケットにする必要はない
- **例外的にLv.1〜Lv.2が独立チケットになるケース**: 複数機能で使う共通部品(デザインシステム、共通フック)を先行整備するとき。この場合は「どの機能で使う予定か」をチケットに明記しておくと、後から使われずに放置される事故を防げる

---

## 追記: Lv.3(画面レベル)チケットの「完了条件の書き方」5段階

Lv.3のチケット自体の粒度(どこまでを1枚にするか)とは別に、**その1枚の中の完了条件をどこまで詳しく書くか**にも粒度があります。書きすぎるとPOの負担が増え、書かなさすぎるとメンバー間で解釈がズレます。同じチケット([チケット詳細画面](front-end/src/pages/detail/App.tsx)の実装)を例に、5段階で比較します。

### 段階1: ゴール/価値レベル(1文)

> チケット詳細画面で、チケットの内容を確認できる。

- 何を作るべきかの方向性しか示していない。「確認できる」の中身(ステータス変更や削除を含むか)はメンバーの解釈次第
- **向いている相手**: 仕様の穴埋めも含めて任せられる、ドメイン理解のあるシニアメンバー

### 段階2: 主要機能の列挙レベル

> - チケットの詳細情報(タイトル・詳細・担当者・ステータス)が表示される
> - ステータスを変更できる
> - チケットを削除できる
> - 一覧画面に戻れる

- 「何ができれば良いか」を機能単位で列挙。UIの見た目や細かい挙動はまだ書かれていない
- **向いている相手**: 機能の範囲さえ決まっていれば、UIやコードの組み方は自分で判断できるメンバー

### 段階3: 画面要素+基本動作レベル

> - URLの`:id`に対応するチケットが表示される。存在しないIDの場合は「チケットが見つかりません」と表示する
> - ステータス変更ボタン(未着手/対応中/完了)を押すと、その場でバッジ表示が切り替わる
> - 削除ボタンを押すと、確認なしで削除され、一覧画面に遷移する
> - 「← 一覧に戻る」リンクで一覧画面に戻れる

- 画面上のUI要素(ボタン・リンク・表示文言)まで具体的に指定。段階2より解釈の余地が減る
- **向いている相手**: 一般的な粒度。多くのチームでの標準的な受け入れ条件の書き方

### 段階4: Given/When/Thenレベル

> - Given: 存在するチケットIDでアクセスしたとき / When: ページを開く / Then: タイトル・詳細・担当者・ステータスバッジが表示される
> - Given: 詳細画面を開いている状態で / When: 「対応中」ボタンを押す / Then: 更新APIが呼ばれ、成功後にバッジ表示が「対応中」に変わる
> - Given: 詳細画面を開いている状態で / When: 削除ボタンを押す / Then: 削除APIが呼ばれ、成功後に一覧画面へ遷移する
> - Given: 存在しないチケットIDでアクセスしたとき / When: ページを開く / Then: 「チケットが見つかりません」と表示される

- 状況(Given)・操作(When)・結果(Then)を1組ずつ明示。テストケースにそのまま転用できる粒度
- **向いている相手**: 経験の浅いメンバーや、仕様確認のコミュニケーションコストを減らしたい場合。詳細は[project-management.md](project-management.md)第4章のGiven/When/Then解説を参照

### 段階5: エッジケース・エラー・非機能要件まで含む実装レベル

> - ステータス変更API・削除APIの通信中は、対象ボタンを`disabled`にし、連打による二重リクエストを防ぐ
> - ステータス変更APIが失敗した場合、エラーメッセージを表示し、表示中のステータスは変更前のまま保持する(楽観的更新はしない)
> - 削除APIが失敗した場合、画面遷移させずエラーメッセージを表示する
> - データ取得中はローディング表示(「読み込み中...」)を出す
> - ネットワークエラー時と、APIが200以外を返した場合とで、表示するメッセージを分けるかどうかを明記する

- 正常系だけでなく、通信失敗・多重送信・ローディングなど**非機能面まで仕様として固定**する。ここまで書くと解釈の余地はほぼ残らない
- **向いている相手**: 品質のばらつきを絶対に避けたい重要機能、あるいは経験が浅くエッジケースに気づきにくいメンバー。ただし全チケットをこの粒度で書くとPO側の負荷が非常に大きくなるため、多用は避ける

### 段階の選び方の目安

| 段階 | POが書く負荷 | メンバーへの裁量 | 向いているチーム/場面 |
|---|---|---|---|
| 1〜2 | 小さい | 大きい | 経験豊富なメンバー中心、ドメイン理解が深いチーム |
| 3 | 中程度 | 中程度 | 標準。特に理由がなければここを基本形にする |
| 4 | やや大きい | 小さい | 新メンバーが多い、認識齟齬によるやり直しが多発している |
| 5 | 大きい | ほぼ無し | 決済・個人情報など、失敗が許されない重要機能に限定 |

**基本方針**: デフォルトは段階3を基準にし、チームやメンバーの経験、機能の重要度に応じて段階を上下させる。同じ人・同じチームでも、機能によって書き分けて構わない(例: 通常のCRUD画面は段階3、決済連携画面は段階5)。

---

## 追記2: Lv.3チケットを任せる「エンジニアのレベル」5段階

上の「完了条件の書き方 段階1〜5」は、**誰に渡すかで選ぶもの**でもあります。ここでは実際にLv.3(画面)チケットに取り掛かるエンジニアを5段階に分け、それぞれにどんな画面を任せ、完了条件をどの段階で書くべきかを整理します。例はすべて`front-end/`のサンプルプロジェクトの画面から取っています。

### Lv.E1: 未経験者/研修中(入社〜数ヶ月、Reactに触れ始めたばかり)

> 任せる例: 既存画面と同じパターンを踏襲できるもの。[チケット新規作成画面](front-end/src/pages/new/App.tsx)を、[チケット編集画面](front-end/src/pages/edit/App.tsx)という既に実装済みのお手本を横に置いた状態で実装してもらう。

- `useState`や`useEffect`の基本は理解できるが、既存コードのパターンを真似ないと書けない。API通信・エラーハンドリングは一人だと抜け漏れが出やすい
- **完了条件の書き方**: 段階5(エッジケース・エラー・非機能要件まで明記)。加えて「参考実装: `src/pages/edit/App.tsx`を参照すること」のように、真似るべきコードそのものを指定する
- **フォロー体制**: 着手前にペアプロ・モブで一度一緒に書く。PRレビューは行単位で細かく行う

### Lv.E2: ジュニア(実務半年〜1年、基本的なCRUD画面は一人で書ける)

> 任せる例: [チケット一覧画面](front-end/src/pages/list/App.tsx)のような、既存の型・API・コンポーネントの延長線上で完結する画面(例: 一覧にステータスでの絞り込みフィルタを追加する)。

- 型定義・API関数・コンポーネント・ページ組み込みという一連の流れは一人でこなせる。ただし、想定していないエラーケースや、他画面への影響には気づきにくい
- **完了条件の書き方**: 段階4(Given/When/Then)。正常系・代表的な異常系(エラー時・0件時など)は明示するが、細部の実装方法までは指定しない
- **フォロー体制**: 実装方針だけ着手前に軽くすり合わせ、あとはPRレビューで拾う

### Lv.E3: ミドル(実務1〜3年、機能設計を含めて自走できる)

> 任せる例: [CSV集計画面](front-end/src/pages/upload/App.tsx)のように、新しいライブラリ(`jszip`・`papaparse`)の選定・導入から任せる画面。ゼロから新しいUIパターン(モーダル)を設計する。

- 新しいライブラリの導入や、既存パターンにない新しいUIの実装も、調査しながら自分で進められる。ただし、プロジェクト全体への影響(パフォーマンス、他機能との整合性)まではレビューでの指摘が必要なことがある
- **完了条件の書き方**: 段階3(画面要素+基本動作レベル)。「何が達成されればよいか」を伝え、ライブラリ選定や実装方法は本人に委ねる
- **フォロー体制**: 着手前に方針(使うライブラリ、大まかな設計)だけ相談してもらい、実装はPRレビューのみ

### Lv.E4: シニア(実務3〜5年以上、保守性・拡張性まで考慮できる)

> 任せる例: その機能群で最初に着手する画面。例えば「チケットのCRUD機能」であれば、後続の[新規作成](front-end/src/pages/new/App.tsx)・[編集](front-end/src/pages/edit/App.tsx)画面が真似できる型([`src/types/ticket.ts`](front-end/src/types/ticket.ts))・API呼び出しパターン([`src/api/ticketApi.ts`](front-end/src/api/ticketApi.ts))を最初に固める、[チケット詳細画面](front-end/src/pages/detail/App.tsx)の実装。

- 単に動くだけでなく、後続の画面が真似しやすい設計(共通化しやすいコンポーネント分割、型設計)を意識して実装できる。他メンバーのコードレビューも担う
- **完了条件の書き方**: 段階1〜2(ゴール/主要機能の列挙のみ)。UI・実装方法の詳細は本人の判断に任せる
- **フォロー体制**: 事前の握り合わせは「実現したいこと」の共有のみ。実装後、他メンバーがそのパターンを踏襲できるかを一緒に確認する

### Lv.E5: リード/アーキテクト(チーム全体の設計方針を決める側)

> 任せる例: サンプルプロジェクトでいう、SPAルーターをやめてMPA構成(ページごとに独立したHTML/App.tsx/main.tsx、URLリライトの仕組み)に切り替えた最初の1画面([`src/pages/list/`](front-end/src/pages/list/App.tsx)+[`vite.config.ts`](front-end/vite.config.ts)のリライト設計)のように、以後のチームメンバー全員が従うことになる技術的な型そのものを作る。

- 個々の画面よりも、プロジェクト全体の構成(ディレクトリ構成、ルーティング方式、状態管理の指針)を決める役割。Lv.3チケット単体を渡されることは少ないが、渡す場合は「新しい設計パターンを確立する」意味を持つ
- **完了条件の書き方**: 段階1(ゴールのみ)、あるいは「完了条件」ではなく「解決したい制約」を伝える(例:「GA4のpage_viewが素直に計測できる構成にしてほしい」)。実現方法自体の設計を任せる
- **フォロー体制**: 事後に設計判断の理由をチームに共有してもらう(ADR: Architecture Decision Recordのような形で残すとなお良い)。POは細かい実装のレビューはせず、決まった方針が他メンバーにも展開されているかを確認する

### まとめ表: エンジニアレベル × 完了条件の段階

| エンジニアレベル | 完了条件の書き方(段階) | フォロー体制 |
|---|---|---|
| Lv.E1 未経験者/研修中 | 段階5(エッジケースまで明記+参考実装指定) | ペアプロ・行単位レビュー |
| Lv.E2 ジュニア | 段階4(Given/When/Then) | 着手前すり合わせ+PRレビュー |
| Lv.E3 ミドル | 段階3(画面要素+基本動作) | 方針確認+PRレビュー |
| Lv.E4 シニア | 段階1〜2(ゴール/機能列挙) | ゴール共有のみ、事後確認 |
| Lv.E5 リード/アーキテクト | 段階1(ゴールまたは制約のみ) | 事後の設計共有(ADR等) |

**運用のコツ**: 同じ画面(Lv.3チケット)でも、誰に渡すかで完了条件の書き込み量を変えてよい。逆に言えば、**いつも同じ書き方(例えば常に段階3)で通している場合、実は一部のメンバーには物足りず、一部のメンバーには不親切になっている可能性がある**。アサインする相手のレベルに合わせて完了条件の粒度を意識的に調整すると、POの記述コストとメンバーの手戻りの両方を最適化できる。
