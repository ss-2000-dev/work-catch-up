# 完了の定義(Definition of Done)ガイド 〜フロントエンド / バックエンド 実例集〜

対象読者:アジャイル(スクラム)でのプロジェクト管理に慣れておらず、「このチケットはどうなったら完了なのか」「スプリントに何を積むべきか」が定まっていない人向け。日本企業の一般的な受託・自社開発の現場で実際に使われている粒度で書いています。

**前提:このプロジェクトでは工数の都合上、自動テスト(ユニットテスト・E2Eテスト)は実施しない方針**。そのため本ドキュメントの完了条件はテストコードを前提としていません。ただし「テストを書かない=動作確認をしない」ではないので、その代替となる確認の型を第5章にまとめています。

---

## 1. 「完了」には2種類ある(ここの混同が一番の原因)

チケットが完了しない・認識がズレる原因のほとんどは、この2つを分けていないことにあります。

| 種類 | 何を定めるか | 誰が決めるか | 単位 |
|---|---|---|---|
| **受け入れ条件(Acceptance Criteria)** | *そのチケット固有*の「何が satisfied されれば要件を満たすか」 | PO(開発が草案を書いてPOが承認) | チケットごとに毎回違う |
| **完了の定義(Definition of Done)** | *全チケット共通*の「どんな状態になったらDoneに動かしてよいか」 | 開発チーム(POと合意) | プロジェクトで1つ、使い回す |

- 受け入れ条件の例:「未ログインで注文ボタンを押すとログイン画面に遷移する」
- 完了の定義の例:「PRがレビュー承認されている」「検証環境にデプロイ済み」「API仕様書を更新済み」

**受け入れ条件は毎回チケットに書く。完了の定義はチケットテンプレートに固定で入れておく。** この分離ができると、プランニングで議論すべき点が「受け入れ条件だけ」に絞られ、会話がかなり楽になります。

> 補足:受け入れ条件の書き方(Given/When/Then)は [project-management.md](project-management.md) の第4章を参照。

---

## 2. スプリントに置くべきチケットの考え方

完了条件の話に入る前に、そもそもチケットの切り方が悪いと何を定義しても完了しません。ここは前提として押さえてください。

### 2-1. 大原則:1スプリント内で「完了」しきれる大きさに切る

スプリントをまたぐチケットは、進捗が見えず、レビューでも見せられず、管理上ほぼ機能しません。目安は以下です。

- **1チケット = 1人が着手して2〜3日以内に終わる大きさ**(1日で終わるものが理想)
- 5日以上かかりそうなら分割を検討する
- スプリントの残り日数より大きいチケットは、そのスプリントに積まない

### 2-2. 縦に切る(機能単位)、横に切らない(工程単位)

初心者が一番やりがちな失敗が「工程で切る」ことです。

```
❌ 横切り(工程単位)- スプリント末に何も見せられない
  - チケットA: 会員登録画面のデザイン実装
  - チケットB: 会員登録APIの実装
  - チケットC: 会員登録の結合
  → Aだけ終わっても価値はゼロ。Cが遅れると全部が未完了になる

✅ 縦切り(機能単位)- 単体で価値が確認できる
  - チケットA: メールアドレスとパスワードで会員登録できる(画面+API+DB)
  - チケットB: 会員登録時にバリデーションエラーを表示する
  - チケットC: 会員登録完了メールを送信する
  → Aが終われば「登録できる」という価値が動く形で見せられる
```

ただし現場によってはフロント担当・バック担当が分かれていて縦切りが難しいこともあります。その場合は「同一スプリント内でフロントとバックのチケットをペアで積む」「バックのAPIチケットを数日先行させる」など、**セットで同じスプリントに入れる**運用にしてください。片方だけ積むと必ず待ちが発生します。

### 2-3. スプリントに積むチケットの内訳(目安)

全部を新規機能で埋めないのがコツです。

| 種別 | 割合の目安 | 内容 |
|---|---|---|
| 機能開発 | 60〜70% | POが求める価値のあるもの |
| 不具合修正 | 10〜20% | 前スプリントからの持ち越し含む |
| 技術的負債・改善 | 10〜20% | リファクタ、ライブラリ更新、環境整備 |
| バッファ | 10〜20% | 割り込み・調査対応の余地 |

**バッファをゼロにしない**こと。100%詰めると必ず溢れて、溢れた分が「未完了」として毎スプリント積み上がります。

### 2-4. スプリントに積む前のチェック(Definition of Ready)

以下を満たさないチケットはスプリントに積まないと決めておくと、スプリント中の停止が激減します。

- [ ] 受け入れ条件が書かれている
- [ ] 仕様の不明点が残っていない(残っているなら、まず「調査チケット」として積む)
- [ ] 依存する他チケット・外部要因が明確になっている
- [ ] 見積もり(工数 or ストーリーポイント)がついている
- [ ] 1スプリント内で終わる大きさになっている

「仕様が固まっていないので調べる」も立派なチケットです。ただし調査チケットは**成果物(調査結果メモ・方針決定)を完了条件にする**こと。時間で区切る(タイムボックス)のも有効です。

---

## 3. 共通の完了の定義(フロント・バック共通)

まずは全チケット共通のベースラインです。これをチケットテンプレートに貼っておきます。

```markdown
## 完了の定義(共通)
- [ ] 受け入れ条件をすべて満たしている
- [ ] コードがリポジトリのコーディング規約に沿っている(Lint / Formatter がエラーなく通る)
- [ ] Pull Request を作成し、1名以上のレビュー承認を得ている
- [ ] レビュー指摘に対応済み(対応しないものは理由をコメントで残している)
- [ ] main(develop)ブランチにマージ済み
- [ ] 検証環境(ステージング)にデプロイし、実機で動作確認済み
- [ ] 動作確認の証跡(スクリーンショット / 画面録画 / 実行ログ)をチケットに添付している
- [ ] 仕様と実装に差異がある場合、仕様書側を更新している
- [ ] 既存機能を壊していないことを確認済み(影響範囲の手動確認)
- [ ] チケットのステータスを更新し、実績工数を記入している
```

日本企業でよく追加される項目(プロジェクトの性質に応じて取捨選択):

- [ ] 設計書 / 基本設計書・詳細設計書を更新済み(受託開発・SIer案件では納品物になるため必須)
- [ ] 課題管理表 / 変更管理表に反映済み
- [ ] リリースノート(変更履歴)に記載済み
- [ ] セキュリティチェックリストを確認済み
- [ ] PO(または業務担当者)による受け入れ確認を得ている

---

## 4. フロントエンド / バックエンド別の完了条件

### 4-1. フロントエンドの完了条件

```markdown
## 完了の定義(フロントエンド)

### 実装
- [ ] 画面がデザイン(Figma等)通りに実装されている
- [ ] デザインと差異がある箇所は、デザイナー/POと合意が取れている
- [ ] レスポンシブ対応が完了している(PC / タブレット / スマートフォン ※対象は案件による)
- [ ] 対象ブラウザで表示・動作を確認済み(例:Chrome / Safari / Edge の最新版)
- [ ] ハードコードした仮データ(モックデータ)が残っていない
- [ ] console.log などのデバッグコードを削除している
- [ ] 未使用のコンポーネント・import・CSSを残していない

### 状態と挙動
- [ ] ローディング中の表示が実装されている(スピナー / スケルトン)
- [ ] エラー時の表示が実装されている(通信失敗・サーバーエラー時に画面が壊れない)
- [ ] 空データ時の表示が実装されている(0件のときのメッセージ)
- [ ] 入力バリデーションとエラーメッセージが仕様通りに表示される
- [ ] 二重送信の防止をしている(送信中はボタンを非活性にする等)
- [ ] 画面遷移・戻るボタンの挙動が正しい

### 品質観点
- [ ] ブラウザのコンソールにエラー・警告が出ていない
- [ ] 主要な操作で目立つ表示崩れ・ガタつきがない
- [ ] アクセシビリティの最低限を満たす(ボタンはbutton要素、画像にalt、フォームにlabel)
- [ ] 文言(ラベル・エラーメッセージ)が仕様書と一致している ※日本語の表記ゆれもここで確認

### 証跡
- [ ] 主要画面・主要状態(正常 / エラー / 空 / ローディング)のスクリーンショットをチケットに添付
```

**「これがあれば完了」と言える成果物(フロントエンド)**

1. マージ済みのPull Request(レビュー承認付き)
2. 検証環境で実際に触れる画面
3. 正常系・異常系のスクリーンショット(または短い操作動画)
4. デザインとの差異・積み残しがあればその一覧コメント

### 4-2. バックエンドの完了条件

```markdown
## 完了の定義(バックエンド)

### 実装
- [ ] APIが仕様通りのリクエスト / レスポンス形式で動作する
- [ ] 正常系の動作を確認済み(実行結果をチケットに添付)
- [ ] 異常系の動作を確認済み(バリデーションエラー・権限なし・データ不存在)
- [ ] 適切なHTTPステータスコードを返している(200 / 201 / 400 / 401 / 403 / 404 / 409 / 500)
- [ ] エラーレスポンスの形式がプロジェクト共通仕様に沿っている
- [ ] 認証・認可のチェックが実装されている(他人のデータが取得・更新できないこと)
- [ ] トランザクション境界が適切(途中で失敗したときに中途半端なデータが残らない)

### データベース
- [ ] マイグレーションファイルを作成し、検証環境で適用済み
- [ ] ロールバック手順を確認済み(切り戻しが必要になったときに戻せるか)
- [ ] インデックスの要否を検討済み(検索・結合に使うカラム)
- [ ] N+1クエリが発生していないことを確認済み(発行SQLをログで確認)
- [ ] テーブル定義書 / ER図を更新済み

### 運用・セキュリティ
- [ ] ログ出力が適切(処理の開始終了・エラー内容が追える)
- [ ] ログに個人情報・パスワード・トークンを出力していない
- [ ] 機密情報をコードに直書きしていない(環境変数 / シークレット管理に格納)
- [ ] SQLインジェクション対策(プレースホルダ使用)ができている
- [ ] 入力値のバリデーションをサーバー側でも実施している(フロントだけに任せない)
- [ ] 環境変数を追加した場合、各環境(開発 / 検証 / 本番)への設定依頼・反映が完了している

### ドキュメント
- [ ] API仕様書(OpenAPI / Swagger、または設計書)を更新済み
- [ ] フロントエンド担当に、利用可能になったAPIと変更点を共有済み

### 証跡
- [ ] APIの実行結果(curl / Postman / Swagger UI のレスポンス)をチケットに添付
```

**「これがあれば完了」と言える成果物(バックエンド)**

1. マージ済みのPull Request(レビュー承認付き)
2. 検証環境で叩けるAPIエンドポイント
3. 正常系・異常系のリクエスト/レスポンス実行結果
4. 更新されたAPI仕様書・テーブル定義書
5. マイグレーションファイル(および適用済みの検証環境DB)

---

## 5. 自動テストをやらない前提での補い方

「テストをやらない」という判断自体は工数上ありうる選択ですが、**確認そのものを省略すると、完了条件が「実装したつもり」に劣化します**。テストコードの代わりに、以下を完了条件に組み込んでください。コストは低いのに効果が大きいものだけを挙げています。

### 5-1. 手動確認の結果を「証跡」として残す

口頭の「動きました」は記録に残りません。以下のどれかをチケットに貼るルールにします。

- 画面のスクリーンショット(正常時・エラー時)
- 短い操作動画(30秒程度でよい)
- APIのリクエスト/レスポンスをコピペしたテキスト

これは「品質保証」というより**「言った言わない」を防ぐ記録**として機能します。後から不具合が出たときに「どこまで確認していたか」が分かるだけでも、原因切り分けの時間が大きく変わります。

### 5-2. 確認観点を最低限テンプレ化する

毎回考えると抜けるので、確認する軸を固定します。

| 観点 | 確認すること |
|---|---|
| 正常系 | 想定通りの入力で想定通りの結果になるか |
| 異常系 | 入力が不正なとき、適切なエラーが返る / 表示されるか |
| 境界値 | 0件、1件、最大文字数、上限を超えた場合 |
| 権限 | 権限のないユーザーが操作できないか |
| 影響範囲 | 今回触った箇所を使っている**既存の画面・APIが壊れていないか** |

特に最後の「影響範囲」だけは省略しないでください。自動テストがない環境では、既存機能の破壊(デグレ)が唯一かつ最大のリスクです。PRの説明欄に「影響範囲」の項目を必須にしておくと、実装者自身が意識するようになります。

### 5-3. テストを書かないことをリスクとして記録しておく

これは管理側としての防衛でもあります。「工数の都合で自動テストは実施しない」という**判断と、その決定者・日付**を課題管理表やこのリポジトリのドキュメントに残しておいてください。後から品質問題が起きたとき、これがあるかないかで話が全く変わります。

```
【決定事項】自動テストの実施範囲について
日時:2026/08/06
決定者:PO 〇〇さん
内容:工数の制約により、本プロジェクトでは自動テスト(ユニット/E2E)を実装しない
代替:各チケットで手動確認を行い、証跡をチケットに添付する運用とする
想定リスク:リグレッション(既存機能の意図しない破壊)の検知が遅れる可能性がある
```

---

## 6. チケットテンプレート(コピーして使う)

```markdown
## 概要
(何を作る/直すのかを1〜3行で)

## 背景・目的
(なぜ必要か。POから聞いたビジネス上の理由)

## 受け入れ条件
- [ ] Given 〜 / When 〜 / Then 〜
- [ ] Given 〜 / When 〜 / Then 〜

## 対象範囲
- 対象: (画面名 / APIエンドポイント / テーブル)
- 対象外: (今回やらないこと。ここを書くのが重要)

## 影響範囲
(この変更で影響を受ける既存機能。確認対象)

## 完了の定義
- [ ] 受け入れ条件をすべて満たしている
- [ ] Lint / Formatter が通る
- [ ] PRレビュー承認済み・マージ済み
- [ ] 検証環境で動作確認済み
- [ ] 動作確認の証跡を添付済み
- [ ] 仕様書 / API仕様書を更新済み
- [ ] 影響範囲の既存機能が壊れていないことを確認済み

## 備考・懸念
(未確定事項、確認待ちのこと)
```

---

## 7. よくあるアンチパターンと対処

| アンチパターン | 何が起きるか | 対処 |
|---|---|---|
| 「実装完了」でDoneにする | レビュー・デプロイが後回しになり、スプリント末に一気に詰まる | 完了の定義に「マージ済み」「検証環境デプロイ済み」を必ず入れる |
| チケットが大きすぎる | スプリントをまたぎ、進捗が見えない | 2〜3日で終わる粒度に分割。分割できないなら調査チケットを先に切る |
| 受け入れ条件が「〜を実装する」 | 実装したかどうかしか判定できず、動くかは分からない | 「〜できる」「〜が表示される」という**利用者視点の結果**で書く |
| 90%完了が続く | 残り10%が毎回スプリントを越える | 完了か未完了かの2値で扱う。90%は未完了 |
| スプリントを100%の工数で埋める | 割り込みで必ず溢れる | 10〜20%のバッファを最初から確保する |
| 未完了チケットを次スプリントにそのまま持ち越す | なぜ終わらなかったかが検証されず、同じことが繰り返される | レトロスペクティブで「なぜ終わらなかったか」を必ず1件は振り返る |
| 完了の定義をチームで合意していない | 人によってDoneの基準が違う | このドキュメントをチームに共有し、レトロで見直す |

---

## 8. まず今スプリントからやる3つ

1. **本ドキュメントの第6章のテンプレートを、チケット作成時のデフォルトにする**(ツールのテンプレート機能に登録する)
2. **次のプランニングで、完了の定義をチームとPOに読み合わせて合意を取る**。合意した日付を記録しておく
3. **「動作確認の証跡をチケットに貼る」だけを先に徹底する**。全部を一度に導入せず、効果が大きいものから1つずつ

完了の定義は一度決めて終わりではなく、**レトロスペクティブで育てていくもの**です。運用してみて重すぎたら項目を削り、事故が起きたら項目を足す。最初から完璧を目指す必要はありません。
