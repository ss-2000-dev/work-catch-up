## Part 1: 概念整理

### 要求・要件・仕様の違い

| 言葉     | 視点    | 問い                | 書き方                 |
| ------ | ----- | ----------------- | ------------------- |
| **要求** | ユーザー  | 何に困っているか・何を達成したいか | 曖昧でよい・自然言語          |
| **要件** | システム  | システムは何をすべきか       | SHALL文で書く・測定可能      |
| **仕様** | エンジニア | システムはどう振る舞うか      | 入力・出力・エラーケースまで厳密に定義 |

**具体例（タスク作成機能）**

- **要求:** 「新しいタスクを作って、担当者に知らせたい」
- **要件:** 「タスクを作成できる。アサイン時に担当者へ通知を送らなければならない」
- **仕様:** 「`POST /api/tasks` に `{title, projectId, assigneeId}` を送ると `201` と `Task` オブジェクトを返す。`title` が空なら `400`。`assigneeId` が指定された場合は SES でメールを非同期送信する」

要求 → 要件 → 仕様の順に「ユーザーの言葉」から「機械が読める定義」へと精度が上がっていく。

### 「要求仕様書」とは

要求仕様書（SRS: Software Requirements Specification）は、要求・要件・外部仕様を1つの文書にまとめたもので、主に**発注者と開発者の契約文書**として使われる。

```
要求仕様書の構成
─────────────────────────────────────────
 要求（ユーザーが何に困っているか）
     ↓ トレーサビリティ（対応関係）
 要件（システムが何をすべきか）
     ↓ トレーサビリティ
 外部仕様（入出力・エラーの厳密な定義）
─────────────────────────────────────────
```

「この要求があるから、この要件が生まれ、この仕様になった」という**追跡可能性（トレーサビリティ）**が保たれているのが要求仕様書の核心。

| ドキュメント | 目的 | 読む人 |
|---|---|---|
| 要求定義書 | 課題・ニーズの整理 | PO・ステークホルダー |
| **要求仕様書** | 要求→要件→仕様の一気通貫 | PO・開発チーム・QA |
| API仕様書 | エンドポイントの技術的詳細 | フロントエンド・外部連携先 |
| 設計書 | 内部実装の設計 | バックエンドエンジニア |

---

## Part 2: TaskFlow 要求仕様書

**プロジェクト:** TaskFlow — チーム向けタスク管理SaaS  
**バージョン:** 1.0  
**対象フェーズ:** MVP

---

### トレーサビリティマトリクス

全体の対応関係を一覧で示す。

| 要求 | 要件 | 仕様（エンドポイント / 機能） |
|---|---|---|
| R-1 タスク一覧を把握したい | F-4, F-8 | `GET /api/tasks` |
| R-2 アサインして通知したい | F-4, F-6 | `POST /api/tasks`（通知含む） |
| R-3 ステータスを更新・共有したい | F-5 | `PATCH /api/tasks/:id/status` |
| R-4 プロジェクトごとに管理したい | F-2, F-3 | `POST /api/projects`, `POST /api/projects/:id/members` |
| R-5 自分のタスクだけ見たい | F-7 | `GET /api/tasks?assigneeId=me` |
| NF-1 レスポンス p99 500ms 以下 | — | ALB + ECS オートスケール |
| NF-2 稼働率 99.9% 以上 | — | Aurora マルチAZ + ECS マルチタスク |
| NF-3〜7 セキュリティ | — | Cognito JWT / bcrypt / テナント分離 |

---

### R-1: タスクを一覧で見て誰が何をやっているか把握したい

**対応要件:** F-4（タスクの作成・編集・削除）、F-8（ソート）

#### 仕様: タスク一覧取得

```
GET /api/tasks
Authorization: Bearer <JWT>
```

**クエリパラメータ**

| パラメータ | 型 | 必須 | 説明 |
|---|---|---|---|
| `projectId` | string (UUID) | — | プロジェクトで絞り込み |
| `assigneeId` | string (UUID) \| `"me"` | — | 担当者で絞り込み。`"me"` は自分 |
| `status` | `TODO` \| `IN_PROGRESS` \| `DONE` | — | ステータスで絞り込み |
| `sortBy` | `dueDate` \| `createdAt` \| `status` | — | ソートキー（デフォルト: `createdAt`） |
| `sortOrder` | `asc` \| `desc` | — | ソート順（デフォルト: `desc`） |

**レスポンス（200 OK）**

```json
{
  "tasks": [
    {
      "id": "tsk_abc123",
      "projectId": "prj_xyz",
      "title": "ログイン画面の実装",
      "description": "Cognito連携を含む",
      "status": "IN_PROGRESS",
      "assigneeId": "usr_001",
      "assigneeName": "山田 太郎",
      "dueDate": "2026-05-20",
      "createdBy": "usr_002",
      "createdAt": "2026-05-01T09:00:00Z",
      "updatedAt": "2026-05-10T15:30:00Z"
    }
  ],
  "total": 42
}
```

**エラーケース**

| 条件 | ステータス | code |
|---|---|---|
| JWT が無効・期限切れ | 401 | `UNAUTHORIZED` |
| `projectId` が自テナントに存在しない | 404 | `NOT_FOUND` |
| `sortBy` に不正な値 | 400 | `VALIDATION_ERROR` |

---

### R-2: タスクをアサインして担当者に通知したい

**対応要件:** F-4（タスク作成）、F-6（メール通知）

#### 仕様: タスク作成

```
POST /api/tasks
Authorization: Bearer <JWT>
Content-Type: application/json
```

**リクエストボディ**

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `projectId` | string (UUID) | ✓ | 所属プロジェクト |
| `title` | string | ✓ | 1〜500文字 |
| `description` | string | — | 最大10,000文字 |
| `assigneeId` | string (UUID) | — | 担当者のユーザーID |
| `dueDate` | string (ISO 8601 date) | — | 例: `"2026-06-01"` |

**レスポンス（201 Created）**

作成されたタスクオブジェクト（上記タスク一覧の要素と同構造）

**メール通知の仕様**

- `assigneeId` が指定された場合のみ送信
- SES で非同期送信（メール送信の失敗は `WARN` ログを出すが、201 レスポンスには影響しない）
- 送信内容: タスク名・プロジェクト名・期日・タスクへの直リンク

**エラーケース**

| 条件 | ステータス | code |
|---|---|---|
| JWT が無効・期限切れ | 401 | `UNAUTHORIZED` |
| `projectId` が自テナントに存在しない | 403 | `FORBIDDEN` |
| `title` が空または500文字超過 | 400 | `VALIDATION_ERROR` |
| `assigneeId` がプロジェクトメンバーでない | 400 | `VALIDATION_ERROR` |
| `dueDate` が過去の日付 | 400 | `VALIDATION_ERROR` |

---

### R-3: タスクのステータスを更新してチームに共有したい

**対応要件:** F-5（ステータスは `TODO / IN_PROGRESS / DONE` の3種）

#### 仕様: ステータス更新

```
PATCH /api/tasks/:id/status
Authorization: Bearer <JWT>
Content-Type: application/json
```

**リクエストボディ**

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `status` | `TODO` \| `IN_PROGRESS` \| `DONE` | ✓ | 新しいステータス |

**レスポンス（200 OK）**

更新後のタスクオブジェクト

**ステータス遷移ルール**

```
TODO ──→ IN_PROGRESS ──→ DONE
 ↑                         │
 └─────────────────────────┘
（DONEからTODOへの巻き戻しも可能）
```

すべての遷移を許可する（一方向制限なし）。

**権限ルール**

- タスクの担当者またはプロジェクトオーナーのみ更新できる
- それ以外は `403 FORBIDDEN`

**エラーケース**

| 条件 | ステータス | code |
|---|---|---|
| タスクが存在しない | 404 | `NOT_FOUND` |
| 権限なし | 403 | `FORBIDDEN` |
| `status` に不正な値 | 400 | `VALIDATION_ERROR` |

---

### R-4: プロジェクトごとにタスクをまとめて管理したい

**対応要件:** F-2（プロジェクト作成・編集・削除）、F-3（メンバー招待）

#### 仕様: プロジェクト作成

```
POST /api/projects
Authorization: Bearer <JWT>
Content-Type: application/json
```

**リクエストボディ**

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `name` | string | ✓ | 1〜255文字 |

**レスポンス（201 Created）**

```json
{
  "id": "prj_xyz",
  "tenantId": "tnt_abc",
  "name": "TaskFlow開発",
  "ownerId": "usr_001",
  "createdAt": "2026-05-01T09:00:00Z"
}
```

#### 仕様: メンバー招待

```
POST /api/projects/:id/members
Authorization: Bearer <JWT>
Content-Type: application/json
```

**リクエストボディ**

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `userId` | string (UUID) | ✓ | 招待するユーザーID（同一テナント内） |

**権限ルール:** プロジェクトオーナーのみ招待可能

**エラーケース**

| 条件 | ステータス | code |
|---|---|---|
| プロジェクトオーナー以外が招待 | 403 | `FORBIDDEN` |
| 別テナントのユーザーを招待 | 400 | `VALIDATION_ERROR` |
| すでにメンバーのユーザーを招待 | 409 | `CONFLICT` |

---

### R-5: 自分に関係のあるタスクだけ絞り込んで見たい

**対応要件:** F-7（担当者フィルタリング）

#### 仕様

`GET /api/tasks` のクエリパラメータ `assigneeId=me` を使用する。

- `me` は JWT のサブジェクト（Cognito sub）から解決する
- レスポンス形式は R-1 のタスク一覧取得と同一

---

### 非機能要件の仕様

#### NF-1: レスポンスタイム p99 500ms 以下

- ECS タスクのオートスケールポリシー: CPU 60% 超過で 2分後にスケールアウト
- RDS Aurora リードレプリカへの読み取り分散（タスク一覧は読み取り専用クエリ）
- CloudWatch でレスポンスタイムの p99 を計測し、500ms 超過でアラート発報

#### NF-2: 稼働率 99.9% 以上

- Aurora: マルチAZ 構成（フェイルオーバー時間 < 30秒）
- ECS: 最低2タスク以上を常時起動、ヘルスチェック失敗タスクは自動差し替え
- ALB: ヘルスチェックパス `GET /health` → `200 OK` を確認

#### NF-3〜7: セキュリティ

| 要件 | 実現方法 |
|---|---|
| NF-3 パスワードのハッシュ化 | Cognito が管理（アプリ側でパスワードを保持しない） |
| NF-4 JWT 認証 | `Authorization: Bearer` ヘッダーを全エンドポイントで検証。Cognito の公開鍵で署名検証 |
| NF-5 テナント分離 | 全DBクエリに `WHERE tenant_id = $1` を必須化。TypeORM のグローバルスコープで強制 |
| NF-6 個人情報の暗号化 | メールアドレスは AES-256 で暗号化してDBに保存。検索時は暗号化後の値で比較 |
| NF-7 ゼロダウンタイムデプロイ | ECS Blue/Green デプロイ（CodeDeploy）を使用 |
