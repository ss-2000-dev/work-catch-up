# トレーサビリティマトリクス 具体例: ポイント交換機能

> 自社サービス **MyApp** のエンドユーザーが保有する仮想ポイント「MyPoint」を、
> 提携先の決済サービス **PartnerPay** のポイント「PPポイント」に交換できるようにする。

TaskFlow の要求仕様書と同じ考え方で、**要求 → 要件 → 仕様** の追跡関係を書く。
加えて、「画面はどう表現するか」「開発フェーズが進んだらカラムをどう増やすか」を扱う。

---

## Part 0. 先に結論:カラムは「横に足す」べきか

ご質問の「仕様カラムの中に書くのか、横に新しいカラムをどんどん足すのか」への答えを先に書く。

### 結論:1枚に横へ足し続けるのは2フェーズ先で破綻する。ハイブリッドにする

理由は **多対多になるから**。

```
要件 F-3(交換を申請できる)
  ├─ 画面: SCR-03 交換入力 / SCR-04 交換確認 / SCR-05 処理中 / SCR-06 完了
  ├─ API : API-03 見積取得 / API-04 交換申請 / API-05 交換状況取得
  ├─ ロジック: BL-01 レート計算 / BL-02 単位チェック / BL-05 冪等性
  └─ テーブル: point_exchanges / point_ledger / exchange_rates
```

1行に全部押し込むとセルが多値になり、「どのAPIがどの画面から呼ばれるか」が**表から読めなくなる**。カラムを足せば足すほど情報が消えていく。

### 正しい形:ID体系が本体、表はその索引

トレーサビリティの本体は表ではなく **ID を全成果物に振ること**。表はIDの対応を引くための索引にすぎない。

```
【1枚目】サマリマトリクス（横に広い / 概観・抜け漏れ検出用）
    要求 → 要件 → 画面 → API → データ → 非機能
    ※ セルにはIDだけ書く。詳細は書かない

【2枚目以降】レイヤー別マトリクス（縦に長い / 実作業用）
    画面マトリクス   : SCR-xx を1行として、対応要件・呼ぶAPI・状態を書く
    APIマトリクス    : API-xx を1行として、対応要件・呼び出し元画面・ロジックを書く
    ロジックマトリクス: BL-xx を1行として、ルール・違反時の挙動を書く
    データマトリクス : テーブルを1行として、対応要件を書く
    テストマトリクス : TC-xx を1行として、対応要件・確認手順を書く
```

**サマリは「棚卸し」、レイヤー別は「作業」** と役割を分ける。この分離をしないと、1枚の表が誰にとっても読めないものになる。

### 使うID体系（この文書で使うもの）

| プレフィックス | 対象 | フェーズ |
|---|---|---|
| `R-` | 要求（ユーザーが困っていること） | 要求 |
| `F-` | 機能要件 | 要件 |
| `NF-` | 非機能要件 | 要件 |
| `SCR-` | 画面 | 基本設計（フロント） |
| `API-` | APIエンドポイント | 基本設計（バック） |
| `BL-` | ビジネスルール / ドメインロジック | 詳細設計 |
| `DAT-` | テーブル・データ構造 | 詳細設計 |
| `EXT-` | 外部システム連携 | 基本設計 |
| `OPS-` | 運用・監視・アラート | 運用設計 |
| `TC-` | テスト観点 / 受け入れ確認項目 | テスト |
| `TKT-` | 開発チケット | 実装 |

### 双方向で辿れることが目的

- **前方（要求 → テスト）**: この要求はどこで実装され、どこで確認されるのか → 抜け漏れ検出
- **後方（コード → 要求）**: このコードはなぜ存在するのか → 仕様変更時の影響範囲特定・不要コードの判定

後方が引けると、「この分岐消していいんでしたっけ」が調べられるようになる。ここがトレーサビリティの実利。

---

## Part 1. 前提となるドメイン整理

### 登場するシステム

```
[エンドユーザー]
      │
      ▼
[MyApp]  ─── MyPoint（自社の仮想ポイント。買い物で貯まる）
      │
      │ ① OAuth連携（アカウント紐付け）
      │ ② 交換APIを叩いてポイント付与を依頼
      ▼
[PartnerPay] ─── PPポイント（提携先の決済ポイント）
```

### ビジネスルール（決めておくべきこと）

| 項目 | 決定内容 |
|---|---|
| 交換レート | MyPoint 100pt → PPポイント 90pt（可変。管理者が設定） |
| 交換単位 | 100pt 刻み。最低 100pt |
| 1回あたり上限 | 50,000pt |
| 1日あたり上限 | 100,000pt |
| 1ヶ月あたり上限 | 300,000pt |
| 方向 | MyPoint → PPポイント の一方向のみ（逆交換・取消は不可） |
| 消費順序 | 有効期限が近いポイントから消費（FIFO） |
| 処理方式 | 非同期（申請受付 → キュー → 外部API → 完了/失敗） |
| 失敗時 | MyPoint を全額返還（補償トランザクション） |
| 前提条件 | PartnerPay アカウント連携済み、かつ本人確認（KYC）完了済み |

> **重要:** 「取消は不可」「一方向のみ」といった**やらないことの明記**が、後から仕様が膨らむのを防ぐ。要求仕様書の価値の半分はここにある。

---

## Part 2. 要求（R）

| # | 要求 | 誰の要求か |
|---|------|---|
| R-1 | 貯まったMyPointを、普段使っているPartnerPayのポイントに交換したい | エンドユーザー |
| R-2 | 交換する前に、何ポイントもらえるのかを確認してから確定したい | エンドユーザー |
| R-3 | 交換した履歴と、それぞれの処理結果を後から確認したい | エンドユーザー |
| R-4 | 自分のPartnerPayアカウントを安全に紐付けたい | エンドユーザー |
| R-5 | 交換が今どうなっているのか（処理中/完了/失敗）を知りたい | エンドユーザー |
| R-6 | 不正な大量交換・換金目的の悪用を防ぎたい | 運営（リスク管理） |
| R-7 | 提携条件の変更に応じて交換レートを変更したい | 運営（企画） |
| R-8 | 失敗した交換を調査し、必要なら再実行したい | 運営（CS・保守） |
| R-9 | ポイント残高の増減を後から証跡として追跡したい | 運営（経理・監査） |

---

## Part 3. 要件（F / NF）

### 機能要件

| # | 要件 | 対応要求 |
|---|------|---------|
| F-1 | PartnerPayアカウントをOAuth 2.0（認可コードフロー）で連携・解除できる | R-4 |
| F-2 | 現在のMyPoint残高と、交換可能な残高を表示できる | R-1, R-2 |
| F-3 | 交換ポイント数を入力し、交換を申請できる | R-1 |
| F-4 | 申請前に、適用レートと付与予定PPポイント数を見積として表示できる | R-2 |
| F-5 | 交換申請は非同期で処理し、状態を `PENDING / PROCESSING / COMPLETED / FAILED` で管理する | R-5 |
| F-6 | 交換の処理状況・結果を画面で確認できる | R-5 |
| F-7 | 交換履歴を一覧・詳細で確認できる（申請日時・ポイント数・レート・結果） | R-3 |
| F-8 | 交換単位（100pt刻み・最低100pt）と1回あたり上限（50,000pt）を検証する | R-6 |
| F-9 | 1日・1ヶ月あたりの交換上限を検証する | R-6 |
| F-10 | 本人確認（KYC）未完了のユーザーは交換できない | R-6 |
| F-11 | 管理者は交換レートを設定・変更でき、変更履歴が残る | R-7 |
| F-12 | 管理者は全ユーザーの交換一覧を検索し、失敗した交換を再実行できる | R-8 |
| F-13 | ポイントの増減はすべて元帳（ledger）に記録し、残高は元帳から導出できる | R-9 |
| F-14 | 外部API呼び出しが失敗した場合、消費したMyPointを自動で返還する | R-1, R-9 |
| F-15 | 同一の申請が二重に処理されない（冪等性を保証する） | R-1, R-6 |

### 非機能要件

| # | 要件 | 理由 |
|---|------|------|
| NF-1 | 交換申請APIは 1秒以内にレスポンスを返す（実処理は非同期） | UX |
| NF-2 | 外部API呼び出しは最大3回リトライ（指数バックオフ） | 可用性 |
| NF-3 | PartnerPayのアクセストークンは暗号化して保存し、ログに出さない | セキュリティ |
| NF-4 | ポイント残高の更新はトランザクション内で行い、二重消費を発生させない | 整合性 |
| NF-5 | 全ての交換操作を監査ログに記録し、7年間保持する | コンプライアンス |
| NF-6 | PartnerPay障害時は交換受付を停止し、ユーザーに明示する（縮退運転） | 可用性 |
| NF-7 | 交換失敗率が5%を超えたらアラートを発報する | 運用 |

---

## Part 4. サマリ・トレーサビリティマトリクス（横に広い版）

**用途:「抜けがないか」を見るためだけの表。詳細は書かない。**

| 要求 | 要件 | 画面 | API | ロジック | データ | 外部 | 非機能 |
|---|---|---|---|---|---|---|---|
| R-1 ポイントを交換したい | F-3, F-14, F-15 | SCR-03, 04, 05, 06, 07 | API-04, API-05 | BL-01, 03, 05, 08 | DAT-1, 2 | EXT-2 | NF-1, NF-4 |
| R-2 交換前に確認したい | F-2, F-4 | SCR-01, 03, 04 | API-01, API-03 | BL-01, BL-02 | DAT-1, 3 | — | — |
| R-3 履歴を確認したい | F-7 | SCR-08, SCR-09 | API-06, API-07 | — | DAT-2 | — | — |
| R-4 アカウント連携したい | F-1 | SCR-02, SCR-10 | API-08, 09, 10 | BL-09 | DAT-4 | EXT-1 | NF-3 |
| R-5 処理状況を知りたい | F-5, F-6 | SCR-05, 06, 07, 09 | API-05 | BL-06 | DAT-2 | — | — |
| R-6 不正利用を防ぎたい | F-8, F-9, F-10, F-15 | SCR-03（入力制御） | API-04 | BL-02, 03, 04, 05 | DAT-2, 5 | — | NF-5 |
| R-7 レートを変更したい | F-11 | SCR-11 | API-11, API-12 | BL-01 | DAT-3 | — | NF-5 |
| R-8 失敗を調査・再実行したい | F-12 | SCR-12, SCR-13 | API-13, API-14 | BL-07 | DAT-2, 5 | EXT-2 | NF-7 |
| R-9 増減を追跡したい | F-13 | SCR-09（ユーザー向け） | — | BL-08 | DAT-1 | — | NF-5 |
| — | NF-2, NF-6 | SCR-03（停止時表示） | — | BL-10 | — | EXT-2 | — |

**この表の読み方・使い方**

- 空欄（`—`）が出たら疑う。「R-9 に画面がない」→ 本当に不要か? 経理は何を見るのか?
- 逆に、どの要求からも参照されていない画面・APIがあれば、それは**要求のない実装**。作らない候補。
- セルが5つも6つもIDで埋まったら、その要件は分割すべきサイン。

---

## Part 5. 画面マトリクス（フロントエンドの詳細）

ここがご質問の「フロントはどんな画面が必要か」に当たる部分。**画面IDを1行にした縦持ちの表**にする。

### 5-1. 画面一覧マトリクス

| 画面ID | 画面名 | 対応要件 | 呼ぶAPI | 遷移先 |
|---|---|---|---|---|
| SCR-01 | ポイント残高トップ | F-2 | API-01 | SCR-02, SCR-03, SCR-08 |
| SCR-02 | PartnerPayアカウント連携 | F-1 | API-08, API-09 | SCR-01（連携後） |
| SCR-03 | 交換ポイント入力 | F-3, F-4, F-8, F-9, F-10 | API-01, API-02, API-03 | SCR-04 |
| SCR-04 | 交換内容の確認 | F-3, F-4 | API-03（再取得）, API-04 | SCR-05 |
| SCR-05 | 交換処理中 | F-5, F-6 | API-05（ポーリング） | SCR-06 / SCR-07 |
| SCR-06 | 交換完了 | F-6 | — | SCR-01, SCR-08 |
| SCR-07 | 交換失敗 | F-6, F-14 | — | SCR-03（やり直し）, SCR-08 |
| SCR-08 | 交換履歴一覧 | F-7 | API-06 | SCR-09 |
| SCR-09 | 交換履歴詳細 | F-7, F-13 | API-07 | SCR-08 |
| SCR-10 | 連携解除確認（モーダル） | F-1 | API-10 | SCR-01 |
| SCR-11 | 【管理】交換レート設定 | F-11 | API-11, API-12 | — |
| SCR-12 | 【管理】交換一覧・検索 | F-12 | API-13 | SCR-13 |
| SCR-13 | 【管理】交換詳細・再実行 | F-12 | API-07, API-14 | SCR-12 |

### 5-2. 画面遷移図

```
                    [SCR-01 ポイント残高トップ]
                       │           │           │
        未連携 ────────┘           │           └──────→ [SCR-08 履歴一覧]
            │                      │                            │
            ▼                      ▼                            ▼
    [SCR-02 アカウント連携]   [SCR-03 交換入力]         [SCR-09 履歴詳細]
            │                      │
   （PartnerPayへ遷移→戻り）        ▼
            │              [SCR-04 交換確認]
            └──→ SCR-01           │  「交換を確定する」
                                   ▼
                          [SCR-05 交換処理中]
                            │              │
                    完了 ───┘              └─── 失敗
                      ▼                          ▼
              [SCR-06 交換完了]          [SCR-07 交換失敗]
```

### 5-3. 画面ごとの詳細（フロント実装者が見る表）

各画面について、**表示項目・状態・エラー時の振る舞い**まで定義する。TaskFlowの要求仕様書がAPIについてやっていたことを、画面についてやるイメージ。

#### SCR-03 交換ポイント入力

| 項目 | 内容 |
|---|---|
| 対応要件 | F-3, F-4, F-8, F-9, F-10 |
| 呼ぶAPI | `API-01` 残高取得 / `API-02` 交換可否チェック / `API-03` 見積取得 |
| 主な表示項目 | 現在のMyPoint残高、連携中のPartnerPayアカウント名、入力欄、現在の交換レート、本日/今月の交換可能残枠、付与予定PPポイント（リアルタイム計算） |

**画面の状態**

| 状態 | 表示 |
|---|---|
| ローディング | 残高・レート取得中はスケルトン表示。入力欄は非活性 |
| 通常 | 入力欄と見積を表示。「確認へ進む」ボタンは有効な入力時のみ活性 |
| 未連携（BL-09違反） | 入力欄を出さず、「PartnerPay連携が必要です」と SCR-02 への導線を表示 |
| KYC未完了（BL-04違反） | 入力欄を出さず、本人確認手続きへの導線を表示 |
| 残高0 | 「交換できるポイントがありません」を表示 |
| 提携先障害中（NF-6） | 「現在ポイント交換を停止しています」を表示し、申請を受け付けない |
| 通信エラー | 「読み込みに失敗しました」＋再試行ボタン |

**入力バリデーション（クライアント側）**

| 条件 | メッセージ |
|---|---|
| 未入力 | 交換するポイント数を入力してください |
| 100の倍数でない（BL-02） | 100ポイント単位で入力してください |
| 100pt未満（BL-02） | 100ポイント以上から交換できます |
| 残高超過（BL-03） | 保有ポイントを超えています（保有: 12,300pt） |
| 50,000pt超（BL-03） | 1回の交換は50,000ポイントまでです |
| 日次/月次枠超過（BL-03） | 本日の交換上限に達しています（残り: 30,000pt） |

> クライアント側バリデーションは**UXのため**であって、**サーバー側での検証を省略してよい理由にはならない**。同じルール（BL-02, BL-03）が両方に実装される、という対応関係をマトリクスで明示しておくのが大事。

#### SCR-04 交換内容の確認

| 項目 | 内容 |
|---|---|
| 対応要件 | F-3, F-4 |
| 呼ぶAPI | `API-03` 見積再取得（表示直前に必ず再取得）/ `API-04` 交換申請 |
| 主な表示項目 | 消費MyPoint、適用レート、付与予定PPポイント、連携先アカウント、**「交換後の取消はできません」の注意文** |

**この画面の設計上のポイント**

- **見積は表示直前に再取得する。** 入力画面で取得したレートをそのまま使うと、レート変更をまたいだときに表示と実処理がズレる
- 見積には**有効期限**を持たせる（例: 発行から5分）。期限切れなら再取得してから確定させる
- 「確定する」ボタンは**押下直後に非活性化**する（BL-05 冪等性の第一防波堤）
- `Idempotency-Key` はこの画面に入った時点で生成し、再送時も同じ値を送る

#### SCR-05 交換処理中

| 項目 | 内容 |
|---|---|
| 対応要件 | F-5, F-6 |
| 呼ぶAPI | `API-05` 交換状況取得（2秒間隔でポーリング、最大30秒） |
| 表示 | プログレス表示 +「処理には数分かかる場合があります」 |

**状態遷移と分岐**

| 取得結果 | 遷移 |
|---|---|
| `COMPLETED` | SCR-06 交換完了へ |
| `FAILED` | SCR-07 交換失敗へ |
| `PENDING` / `PROCESSING` が30秒継続 | 「処理は継続中です。結果は履歴画面からご確認いただけます」→ SCR-08 へ誘導 |
| ユーザーが画面を閉じた | 処理はサーバー側で継続する（画面を閉じても中断しない設計であること） |

> **非同期処理をフロントに落とし込むときの典型的な抜け** が「ポーリングが終わらないケース」と「離脱したユーザーへの結果通知」。マトリクスに `F-5 → SCR-05` の行を作って初めて、この検討漏れに気づける。

#### SCR-07 交換失敗

| 項目 | 内容 |
|---|---|
| 対応要件 | F-6, F-14 |
| 表示 | 失敗理由（ユーザー向けの言葉に変換）、**「消費したポイントは返還されました」の明示**、現在の残高、再試行導線 |

**失敗理由の出し分け**

| 内部エラーコード | ユーザーへの表示 |
|---|---|
| `PARTNER_UNAVAILABLE` | 提携先が混み合っています。時間をおいて再度お試しください |
| `PARTNER_ACCOUNT_INVALID` | PartnerPayアカウントの連携が無効です。再連携してください（→SCR-02） |
| `LIMIT_EXCEEDED` | 交換上限に達しています |
| `INTERNAL_ERROR` | 処理に失敗しました。時間をおいて再度お試しください |

> 内部コードをそのまま出さない。**技術的なエラーコードと利用者向け文言の対応表**を持つこと自体が仕様。

#### SCR-08 交換履歴一覧 / SCR-09 詳細

| 項目 | SCR-08 | SCR-09 |
|---|---|---|
| 対応要件 | F-7 | F-7, F-13 |
| 呼ぶAPI | API-06 | API-07 |
| 表示項目 | 申請日時、消費pt、付与pt、ステータスバッジ | 上記＋適用レート、処理完了日時、失敗理由、取引ID |
| 状態 | ローディング / 空（履歴なし）/ エラー / ページング | ローディング / 404 / エラー |

### 5-4. 共通コンポーネント（横断で洗い出す）

画面単位で切ると重複が見えないので、**画面をまたいで使うものを別に洗い出す**。

| コンポーネントID | 名称 | 使用画面 |
|---|---|---|
| CMP-01 | ポイント残高カード | SCR-01, SCR-03 |
| CMP-02 | ステータスバッジ（PENDING/PROCESSING/COMPLETED/FAILED） | SCR-05, 08, 09, 12, 13 |
| CMP-03 | ポイント数フォーマッタ（3桁区切り＋単位） | 全画面 |
| CMP-04 | 提携先障害バナー | SCR-01, SCR-03 |
| CMP-05 | 確認モーダル（取消不可の警告付き） | SCR-04, SCR-10 |

---

## Part 6. APIマトリクス（バックエンドの詳細）

| API-ID | エンドポイント | 対応要件 | 呼び出し元 | ロジック | 主なエラー |
|---|---|---|---|---|---|
| API-01 | `GET /api/points/balance` | F-2, F-13 | SCR-01, 03 | BL-08 | 401 |
| API-02 | `GET /api/exchanges/eligibility` | F-9, F-10 | SCR-03 | BL-03, 04, 09 | 401 |
| API-03 | `POST /api/exchanges/quote` | F-4 | SCR-03, 04 | BL-01, BL-02 | 400, 409(レート変更) |
| API-04 | `POST /api/exchanges` | F-3, F-5, F-8, F-9, F-15 | SCR-04 | BL-01〜05, BL-08 | 400, 402, 403, 409, 503 |
| API-05 | `GET /api/exchanges/:id/status` | F-5, F-6 | SCR-05 | BL-06 | 401, 403, 404 |
| API-06 | `GET /api/exchanges` | F-7 | SCR-08 | — | 401 |
| API-07 | `GET /api/exchanges/:id` | F-7, F-13 | SCR-09, 13 | — | 401, 403, 404 |
| API-08 | `POST /api/partner-accounts/link/start` | F-1 | SCR-02 | BL-09 | 401, 409 |
| API-09 | `POST /api/partner-accounts/link/callback` | F-1 | SCR-02 | BL-09 | 400, 401 |
| API-10 | `DELETE /api/partner-accounts` | F-1 | SCR-10 | BL-09 | 401, 409(処理中の交換あり) |
| API-11 | `GET /api/admin/exchange-rates` | F-11 | SCR-11 | — | 401, 403 |
| API-12 | `PUT /api/admin/exchange-rates` | F-11 | SCR-11 | BL-01 | 400, 403 |
| API-13 | `GET /api/admin/exchanges` | F-12 | SCR-12 | — | 401, 403 |
| API-14 | `POST /api/admin/exchanges/:id/retry` | F-12 | SCR-13 | BL-07 | 403, 404, 409 |

### 中核となる API-04 の仕様（TaskFlow形式）

```
POST /api/exchanges
Authorization: Bearer <JWT>
Idempotency-Key: <UUID>      ← 必須（F-15）
Content-Type: application/json
```

**リクエストボディ**

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `points` | integer | ✓ | 消費するMyPoint。100の倍数、100〜50,000 |
| `quoteId` | string | ✓ | API-03 で発行した見積ID（発行から5分以内） |

**レスポンス（202 Accepted）**

```json
{
  "exchangeId": "exc_01HXYZ",
  "status": "PENDING",
  "points": 10000,
  "rate": 0.9,
  "grantedPoints": 9000,
  "requestedAt": "2026-08-06T10:00:00Z",
  "statusUrl": "/api/exchanges/exc_01HXYZ/status"
}
```

**エラーケース**

| 条件 | ステータス | code |
|---|---|---|
| JWTが無効 | 401 | `UNAUTHORIZED` |
| PartnerPay未連携（BL-09） | 403 | `PARTNER_NOT_LINKED` |
| KYC未完了（BL-04） | 403 | `KYC_REQUIRED` |
| 100の倍数でない / 範囲外（BL-02） | 400 | `VALIDATION_ERROR` |
| ポイント残高不足（BL-03） | 402 | `INSUFFICIENT_POINTS` |
| 日次・月次上限超過（BL-03） | 400 | `LIMIT_EXCEEDED` |
| `quoteId` が期限切れ / レート変更済み | 409 | `QUOTE_EXPIRED` |
| 同一 `Idempotency-Key` で処理中（BL-05） | 409 | `DUPLICATE_REQUEST` |
| 同一 `Idempotency-Key` で処理済み（BL-05） | 200 | 前回の結果をそのまま返す |
| PartnerPay障害中（NF-6） | 503 | `SERVICE_UNAVAILABLE` |

---

## Part 7. ビジネスロジック（BL）マトリクス

**ルールを1行にする表。ここが「バックエンドのロジックをもっと詳細に」の受け皿になる。**

| BL-ID | ルール | 対応要件 | 実装場所 | 違反時 |
|---|---|---|---|---|
| BL-01 | 付与PP = floor(消費MyPoint × レート)。端数切り捨て | F-4, F-11 | ExchangeRateService | — |
| BL-02 | 100の倍数・100pt以上・50,000pt以下 | F-8 | 入力検証（FE/BE両方） | 400 |
| BL-03 | 残高・日次枠・月次枠を満たすこと | F-9 | ExchangeService | 402 / 400 |
| BL-04 | KYC完了済みユーザーのみ交換可 | F-10 | ExchangeService | 403 |
| BL-05 | 同一Idempotency-Keyの二重処理を禁止 | F-15 | Idempotencyミドルウェア | 409 / 前回結果を返す |
| BL-06 | 状態遷移は `PENDING→PROCESSING→COMPLETED/FAILED` のみ | F-5 | ExchangeStateMachine | 不正遷移は例外 |
| BL-07 | 再実行は `FAILED` のもののみ。新しい取引IDで実行 | F-12 | AdminExchangeService | 409 |
| BL-08 | ポイント消費は有効期限が近い順（FIFO）。増減は必ず元帳へ記録 | F-13, F-14 | PointLedgerService | — |
| BL-09 | 連携は1ユーザー1アカウント。処理中の交換があれば解除不可 | F-1 | PartnerAccountService | 409 |
| BL-10 | 外部API失敗時は3回リトライ後にFAILEDとし、ポイントを返還 | F-14, NF-2 | ExchangeWorker | 元帳に返還記録 |

### BL-05 冪等性とBL-10 補償トランザクションの処理フロー

**この機能の心臓部。ここを曖昧にしたまま実装すると、必ずポイントの二重消費か消失が起きる。**

```
[SCR-04 確定ボタン]
   │ POST /api/exchanges  (Idempotency-Key: uuid)
   ▼
① Idempotencyミドルウェア（BL-05）
   ├─ 同キーで処理済み  → 保存済みレスポンスを返す（200）
   ├─ 同キーで処理中    → 409 DUPLICATE_REQUEST
   └─ 新規             → キーをロック付きで登録して次へ
   │
   ▼
② 事前検証（BL-02,03,04,09） → NGなら 4xx で終了
   │
   ▼
③ DBトランザクション開始
   ├─ point_ledger に「消費」レコードを挿入（BL-08）
   ├─ point_exchanges に status=PENDING で挿入
   └─ コミット   ← この時点でユーザーのポイントは減っている
   │
   ▼
④ 202 Accepted を返す（NF-1: 1秒以内）
   │
   ▼ （非同期ワーカー）
⑤ status=PROCESSING に更新（BL-06）
   │
   ▼
⑥ PartnerPay 付与API を呼ぶ（EXT-2）
   │  ※ こちらも冪等キーを渡す（提携先側での二重付与を防ぐ）
   │
   ├─ 成功 → status=COMPLETED、外部取引IDを保存
   │
   └─ 失敗 → 最大3回リトライ（指数バックオフ / NF-2）
              │
              └─ 全滅 → 【補償トランザクション（BL-10）】
                    ├─ point_ledger に「返還」レコードを挿入
                    ├─ status=FAILED、失敗理由を保存
                    └─ 監査ログ出力（NF-5）＋ 失敗率アラート判定（NF-7）
```

**設計上のポイント**

- 残高は `users.point_balance` のようなカラムで持たず、**元帳（ledger）の合算で導出する**（BL-08 / F-13）。返還も「マイナスを打ち消す行」として記録するので、履歴が消えない
- 返還は「消費レコードのDELETE」ではなく「返還レコードのINSERT」。**削除すると監査で追えなくなる**（NF-5）
- 外部API側にも冪等キーを渡す。自システムのリトライが提携先で二重付与にならないようにする
- ⑥が「成功したがレスポンスが届かなかった」ケースが最も厄介。**提携先の取引照会APIで確認してから**リトライする設計にする（EXT-2 に照会APIが必要、という要件がここで発見される）

---

## Part 8. データ・外部連携・運用のマトリクス

### データ（DAT）

| DAT-ID | テーブル | 対応要件 | 主なカラム |
|---|---|---|---|
| DAT-1 | `point_ledger` | F-13, F-14 | id, user_id, type(EARN/CONSUME/REFUND), points, expires_at, ref_exchange_id, created_at |
| DAT-2 | `point_exchanges` | F-3, F-5, F-7 | id, user_id, points, rate, granted_points, status, partner_txn_id, failure_code, idempotency_key, requested_at, completed_at |
| DAT-3 | `exchange_rates` | F-11 | id, rate, valid_from, valid_to, created_by |
| DAT-4 | `partner_accounts` | F-1 | user_id, partner_user_id, access_token(暗号化), refresh_token(暗号化), linked_at |
| DAT-5 | `audit_logs` | NF-5 | id, actor_id, action, target_id, before, after, created_at |

### 外部連携（EXT）

| EXT-ID | 連携先 | 用途 | 対応要件 | 障害時 |
|---|---|---|---|---|
| EXT-1 | PartnerPay OAuth | アカウント連携 | F-1 | 連携画面にエラー表示、リトライ可 |
| EXT-2 | PartnerPay ポイント付与API | ポイント付与 / 取引照会 | F-3, F-14 | 3回リトライ → 返還（BL-10）、受付停止（NF-6） |

### 運用・監視（OPS）

| OPS-ID | 監視項目 | しきい値 | 対応要件 |
|---|---|---|---|
| OPS-1 | 交換失敗率 | 5%超で5分継続 | NF-7 |
| OPS-2 | `PENDING` のまま10分以上滞留した交換の件数 | 1件以上 | F-5, NF-6 |
| OPS-3 | PartnerPay APIのレスポンスタイム | p99 3秒超 | NF-2 |
| OPS-4 | 返還（REFUND）件数の急増 | 前日比200%超 | F-14 |
| OPS-5 | 元帳合算残高と交換実績の日次突合 | 不一致1件以上 | F-13, NF-5 |

---

## Part 9. テスト観点マトリクス（検証トレーサビリティ）

**要件に対して「どこで確認するか」が1対1で埋まっていることが、抜け漏れがないことの証明になる。**

| TC-ID | 確認内容 | 対応要件 | 確認方法 |
|---|---|---|---|
| TC-01 | 100pt刻み・範囲外の入力が弾かれる | F-8 / BL-02 | 画面入力 + API直接 |
| TC-02 | 残高を超える交換が弾かれる | F-9 / BL-03 | API直接 |
| TC-03 | 日次上限到達後に交換できない | F-9 / BL-03 | 上限まで交換して確認 |
| TC-04 | KYC未完了ユーザーが交換できない | F-10 / BL-04 | ユーザー状態を変えて確認 |
| TC-05 | 同一Idempotency-Keyの二重送信で二重消費が起きない | F-15 / BL-05 | 同時2リクエスト送信 |
| TC-06 | 確認ボタン連打で交換が2件作られない | F-15 | 画面操作 |
| TC-07 | 外部API失敗時にポイントが全額返還される | F-14 / BL-10 | 外部APIをモックで失敗させる |
| TC-08 | 失敗理由がユーザー向け文言で表示される | F-6 | SCR-07 の表示確認 |
| TC-09 | 処理中に画面を閉じても処理が完了する | F-5 | 画面を閉じて履歴で確認 |
| TC-10 | レート変更後の見積で古いレートが適用されない | F-11 / BL-01 | 見積取得→レート変更→確定 |
| TC-11 | 元帳の合算と表示残高が一致する | F-13 / BL-08 | 複数取引後に突合 |
| TC-12 | 連携解除後に交換できない | F-1 / BL-09 | 解除して確認 |
| TC-13 | アクセストークンがログに出力されていない | NF-3 | ログをgrep |
| TC-14 | 提携先障害中に受付が停止される | NF-6 | 外部APIを停止して確認 |
| TC-15 | 管理者が失敗した交換を再実行できる | F-12 / BL-07 | SCR-13 から実行 |

> **カバレッジチェックの使い方:** F-1〜F-15、NF-1〜NF-7 の全IDが、TC のどこかに1回は出てくるか。出てこない要件は**確認されずにリリースされる要件**。

---

## Part 10. チケット（TKT）へのマッピング

マトリクスの最終列が、スプリントに積むチケットになる。ここまで来ると「何をチケットにするか」で悩まなくなる。

| TKT-ID | チケット名 | 対応 | 領域 | 目安 |
|---|---|---|---|---|
| TKT-01 | 元帳テーブル・残高導出ロジックの実装 | DAT-1, BL-08 | BE | 3日 |
| TKT-02 | 交換レートマスタと見積API | DAT-3, API-03, BL-01 | BE | 2日 |
| TKT-03 | PartnerPay OAuth連携（開始・コールバック・解除） | API-08〜10, EXT-1 | BE | 3日 |
| TKT-04 | 交換申請API（検証・元帳記録・202返却） | API-04, BL-02〜05 | BE | 3日 |
| TKT-05 | 非同期ワーカーと外部API連携・補償処理 | BL-10, EXT-2 | BE | 3日 |
| TKT-06 | 交換状況・履歴API | API-05〜07 | BE | 2日 |
| TKT-07 | 残高トップ画面 | SCR-01, CMP-01 | FE | 1日 |
| TKT-08 | アカウント連携画面 | SCR-02, SCR-10 | FE | 2日 |
| TKT-09 | 交換入力画面（バリデーション・見積表示） | SCR-03 | FE | 3日 |
| TKT-10 | 交換確認・処理中・完了・失敗画面 | SCR-04〜07 | FE | 3日 |
| TKT-11 | 交換履歴一覧・詳細画面 | SCR-08, SCR-09 | FE | 2日 |
| TKT-12 | 管理画面：レート設定 | SCR-11, API-11, 12 | FE/BE | 2日 |
| TKT-13 | 管理画面：交換一覧・再実行 | SCR-12, 13, API-13, 14 | FE/BE | 3日 |
| TKT-14 | 監査ログ・監視アラート設定 | NF-5, OPS-1〜5 | BE/Infra | 2日 |

### スプリントへの並べ方（依存関係を考慮）

```
スプリント1: TKT-01, TKT-02, TKT-03           （土台。ここが無いと何も動かない）
スプリント2: TKT-04, TKT-06, TKT-07, TKT-08   （BE先行 + FEが追いかける）
スプリント3: TKT-05, TKT-09, TKT-10           （中核フロー完成 = 交換が通る）
スプリント4: TKT-11, TKT-12, TKT-13, TKT-14   （周辺・運用）
```

**ポイント:** スプリント3の終わりで初めて「ユーザーがポイントを交換できる」が成立する。POには**この時点でデモできる**と最初に伝えておく。スプリント1・2は動くものが見せづらいので、そこを事前に合意しておかないと「2スプリント使って何もできていない」という誤解が生まれる。

---

## Part 11. まとめ:カラムの増やし方の指針

| 場面 | やること |
|---|---|
| 要求・要件が固まった | サマリマトリクス（Part 4）を作る。カラムは「要件」まで |
| 基本設計に入った | サマリに `画面 / API / データ / 外部` の列を足す。**IDのみ**書く |
| 画面設計を始めた | **サマリには足さず**、画面マトリクス（Part 5）を新しい表として作る |
| ロジックを詰める | ビジネスロジックマトリクス（Part 7）を新しい表として作る |
| テスト計画を作る | テスト観点マトリクス（Part 9）を作り、全要件IDが登場するか確認する |
| 実装に入る | チケットマトリクス（Part 10）を作り、チケットに対応IDを必ず書く |

### 判断基準

> **1つのセルにIDが3つ以上入り始めたら、そのフェーズは別テーブルに切り出す。**

この一線だけ守れば、マトリクスは破綻しない。

### 運用面の現実的な話

- 行数が50を超えたらMarkdownの表は限界。**スプレッドシート or Notionのデータベース**に移す
- Jira/Backlog を使っているなら、チケットのカスタムフィールドに「対応要件ID」を1つ作るだけでも後方トレーサビリティは成立する
- 全部を完璧に維持しようとしない。**維持するのは「要件ID ↔ チケットID」の対応だけでも効果が出る**。仕様変更が来たときに「どのチケットで作った機能か」が即座に引ける状態が最低ライン
