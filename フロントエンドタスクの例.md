# フロントエンドタスクの例 〜チケットの切り方による違い〜

対象読者: SCM/POとしてフロントエンドメンバーにチケットを振る人向け。同じ機能追加でも、チケットの切り方(工程で分けるか/機能で分けるかで、完了条件・作業手順・見えるリスクがどう変わるかを、実際に手を動かしたサンプルプロジェクト(`front-end/`のチケット管理アプリ)を題材に比較します。

背景の考え方は [definition-of-done.md](definition-of-done.md) 第2-2章「縦に切る(機能単位)、横に切らない(工程単位)」を参照してください。本ドキュメントはそこで説明されている2つの切り方を、実プロジェクトのチケット文面レベルまで具体化したものです。

---

## 0. 題材にする新機能

`front-end/`のチケット管理アプリに、**「チケット詳細画面にコメントを投稿・一覧表示できる機能」**を追加する、というシナリオで統一します。

- 対象画面: [src/pages/detail/App.tsx](front-end/src/pages/detail/App.tsx)(既存のチケット詳細ページ)
- 現状は存在しない、コメント一覧表示 + コメント投稿フォームを新規追加する
- バックエンド(json-server)には、コメント用のエンドポイントがまだ無い前提とする(`db.json`に`comments`配列を追加する必要がある)

---

## パターンA: 現状のやり方(工程で分割 = 横切り)

「〜のUI実装」「〜の画面処理実装」のように、**画面の見た目**と**通信・ロジック**を別チケットにする現状のやり方です。

### チケットA-1:「チケット詳細画面 コメント欄のUI実装」

**完了条件**
- コメント一覧を表示するエリアが、チケット詳細画面に追加されている(ダミーの固定データでよい)
- コメント投稿用の入力欄と送信ボタンが表示されている
- 送信ボタンを押した時の見た目(押せる/押せない、ローディング中の表示)がある
- 実際にAPIと繋がっていなくてよい(クリックしてもコメントは増えない)
- レスポンシブ・崩れがないことを目視確認済み

**作業手順(コードレベル)**
1. `src/types/comment.ts` を作成し、`Comment`型(id, ticketId, author, body, createdAt)を仮で定義する
2. `src/components/CommentList.tsx` を作成する。propsで受け取った`Comment[]`をダミー配列(`const dummyComments = [...]`)で表示確認する
3. `src/components/CommentForm.tsx` を作成する。`useState`で入力欄(投稿者・本文)の値を保持し、送信ボタンを押しても`console.log`するだけにしておく
4. `src/pages/detail/App.tsx`に`CommentList`・`CommentForm`を組み込み、レイアウトを整える
5. 見た目をブラウザで確認し、PRを出す(この時点ではコメントは投稿・保存されない)

**このパターンの問題点**
- このチケットが完了しても「コメントを投稿できる」という価値はゼロ(見た目だけ)
- レビュー時に「これはダミーデータです」という前提を都度説明する必要がある
- 後続のA-2が別スプリントに持ち越されると、動かないUIだけが検証環境に残る

---

### チケットA-2:「チケット詳細画面 コメント投稿処理の実装」

前提: チケットA-1が完了していること(依存関係あり)。

**完了条件**
- `GET /tickets/:id/comments` でコメント一覧を取得し、A-1で作ったUIに実データが表示される
- `POST /tickets/:id/comments` で投稿すると、一覧に新しいコメントが反映される
- 通信中はボタンが無効化され、二重送信されない
- 通信エラー時にエラーメッセージが表示される
- `db.json`にコメント用のダミーデータが追加されている

**作業手順(コードレベル)**
1. `db.json`に`comments`配列(ticketIdで紐付け)を追加し、`json-server`で疑似APIを立てられるようにする
2. `src/api/commentApi.ts`を作成し、`fetchComments(ticketId)` / `createComment(ticketId, input)`を実装する(実装方針は既存の[src/api/ticketApi.ts](front-end/src/api/ticketApi.ts)を踏襲)
3. [src/pages/detail/App.tsx](front-end/src/pages/detail/App.tsx)で`useEffect`を追加し、マウント時に`fetchComments`を呼んで`useState`にセットする
4. `CommentForm`の送信処理(A-1では`console.log`だった部分)を、実際に`createComment`を呼ぶ処理に差し替える
5. 送信中フラグ(`isSubmitting`)を`useState`で持ち、ボタンの`disabled`に反映する
6. エラー時は`try/catch`でエラーメッセージ用のstateにセットして表示する
7. 動作確認(コメント投稿→一覧に反映されるか、ページ再読み込みでも保持されているか)、PRを出す

---

## パターンB: 変更後のやり方(機能単位で1チケットに統合 = 縦切り)

UIと通信・ロジックを分けず、「コメント機能」という**ユーザーに価値が見える単位**で1チケットにします。

### チケットB:「チケット詳細画面でコメントを投稿・閲覧できるようにする」

**受け入れ条件(Acceptance Criteria)**
- チケット詳細画面を開くと、そのチケットに紐づくコメントが投稿日時の新しい順に表示される
- 投稿者名・本文を入力して送信すると、一覧に反映される(ページ再読み込み後も残っている)
- 本文が空の場合は送信できない(送信ボタンを無効化 or バリデーションメッセージ表示)
- 通信中はボタンが無効化され、連打しても二重投稿されない
- 通信エラー時は、その旨がユーザーにわかる形で表示される

**完了条件(Definition of Done、プロジェクト共通)**
[definition-of-done.md](definition-of-done.md) のプロジェクト共通DoDに準拠(PRレビュー承認、検証環境での動作確認など)。

**作業手順(コードレベル)**
1. `db.json`に`comments`配列を追加する
2. `src/types/comment.ts`で`Comment`型・`CommentInput`型を定義する(参考: [src/types/ticket.ts](front-end/src/types/ticket.ts))
3. `src/api/commentApi.ts`で`fetchComments` / `createComment`を実装する(参考: [src/api/ticketApi.ts](front-end/src/api/ticketApi.ts) [src/api/client.ts](front-end/src/api/client.ts))
4. `src/components/CommentList.tsx`を作成する。propsで受け取った`Comment[]`を新しい順に並び替えて表示する
5. `src/components/CommentForm.tsx`を作成する。入力値を`useState`で保持し、`onSubmit`propsで渡された関数を呼ぶ(値の保持の実装は、既存の[src/components/TicketForm.tsx](front-end/src/components/TicketForm.tsx)と同じ考え方)
6. [src/pages/detail/App.tsx](front-end/src/pages/detail/App.tsx)で、
   - `useEffect`でマウント時に`fetchComments`を呼び`useState`にセット
   - `CommentForm`の`onSubmit`で`createComment`を呼び、成功したらコメント一覧のstateに追加(または再取得)
   - 送信中・エラーのstateを持ち、UIに反映
7. 空欄バリデーション(送信ボタンの`disabled`条件、またはエラーメッセージ)を実装する
8. ブラウザで一通り動作確認(投稿→表示→再読み込みしても残っているか、エラー時の表示、連打時に二重投稿されないか)し、PRを出す

**このパターンの利点**
- 1チケットが終わった時点で「コメントを投稿できる」という価値がそのまま動く形で確認できる
- レビューやデモで「ダミーです」という前置きが不要
- 依存関係が発生しないので、途中でスプリントをまたいでも「半分だけ動くUI」が残らない

---

## 比較まとめ

| 観点 | パターンA(工程で分割) | パターンB(機能単位) |
|---|---|---|
| チケット数 | 2枚(UI実装/処理実装) | 1枚 |
| 依存関係 | A-2はA-1完了が前提 | なし |
| 1枚完了時点の価値 | A-1単体では価値なし(見た目だけ) | 完了=そのまま使える機能 |
| レビューのしやすさ | 「ダミーです」の説明が要る | 実際に動く状態でレビューできる |
| 見積もりのブレやすさ | UI/処理を別々に見積もれるので細かい単位になりやすい | 1枚の見積もりが大きくなりがち(3日超なら分割検討) |
| 向いているケース | 経験の浅いメンバーに、まず見た目だけ先に着手させたい場合など | 通常の機能開発全般(推奨) |

**SCM/POとしての判断の目安**: 基本はパターンB(機能単位)を推奨します。[definition-of-done.md](definition-of-done.md)にもある通り、1チケットが1〜3日で終わる大きさに収まるなら、工程で分ける理由はあまりありません。ただし、メンバーの経験が浅くUI実装だけ先に慣れさせたい、あるいはAPI側の仕様が固まっておらずUIだけ先行着手させたい、といった事情がある場合は、パターンAのように意図的に分割し、その際は「A-1はA-2とセットで同じスプリントに積む」ことを徹底してください(片方だけ積むと動かないUIが検証環境に残ります)。

---

## 題材2: 「zip化CSVをアップロードして仮想ポイントを集計・モーダル表示する」機能

実際に`front-end/`に実装した [`/upload`](front-end/src/pages/upload/App.tsx) 画面を題材にした例です。バックエンドAPIを使わず、**ブラウザ内でzip解凍・CSVパース・集計まで完結する**という点が、パターン1(コメント機能)との違いです。

### パターンA: 現状のやり方(工程で分割 = 横切り)

#### チケットA-1:「CSV集計画面 UI実装」

**完了条件**
- `/upload`でファイル選択(zip)欄とアップロードボタンが表示されている
- アップロードボタンを押すと、モーダルが開き、固定のダミー値(例: 対象ユーザー数100件、仮想ポイント合計50,000pt)が表示される
- モーダルの閉じるボタン・オーバーレイクリックで閉じる動作が実装されている
- zip解凍やCSVの中身の解析は行っていなくてよい

**作業手順(コードレベル)**
1. `upload.html`・`src/pages/upload/main.tsx`を作成し、`vite.config.ts`の`build.rollupOptions.input`にエントリを追加、`cleanUrlRewrite`に`/upload → /upload.html`のリライトを追加する
2. `src/components/Modal.tsx`を作成する(`isOpen` / `onClose` / `children`を受け取る汎用コンポーネント)
3. `src/pages/upload/App.tsx`を作成する。`useState`でファイル選択・モーダル開閉を保持し、アップロードボタン押下時は`aggregatePointsFromZip`を呼ぶ代わりに固定値(ダミー)を`useState`にセットしてモーダルを開く
4. [src/pages/list/App.tsx](front-end/src/pages/list/App.tsx)から`/upload`へのリンクを追加する
5. ブラウザで見た目・開閉動作を確認し、PRを出す

**このパターンの問題点**
- ダミー値が正しいUIレイアウトの確認にしかならず、「集計が合っているか」は誰も確認できない
- 実際にはCSVの列がズレている・ヘッダー行の有無など、集計ロジック側で起きる考慮漏れの方がバグになりやすいが、このチケットではそこに触れない

#### チケットA-2:「CSV集計画面 zip解凍・集計処理の実装」

前提: チケットA-1が完了していること(依存関係あり)。

**完了条件**
- 実際にzipファイルをアップロードすると、中のCSVが解凍・パースされ、C列(仮想ポイント)の合計と行数(対象ユーザー数)が正しくモーダルに表示される
- 1行目はヘッダー行としてスキップされる
- zip内にCSVファイルが無い場合、パースに失敗した場合にエラーメッセージが表示される
- 処理中はボタンが無効化され、連打で二重に処理が走らない

**作業手順(コードレベル)**
1. `jszip`・`papaparse`(および`@types/papaparse`)を`package.json`に追加し、`npm install`する
2. `src/utils/parseZipCsv.ts`を作成する。`JSZip.loadAsync`でzipを開き、拡張子`.csv`のファイルを探して`async("string")`でテキスト化、`Papa.parse`でパースし、ヘッダー行を除いたC列(`POINT_COLUMN_INDEX = 2`)を合計する`aggregatePointsFromZip`関数を実装する
3. A-1で仮に入れていたダミー値のセット処理を、`aggregatePointsFromZip`の呼び出しに差し替える
4. `try/catch`でエラーをキャッチし、`useState`のエラーメッセージにセットしてUIに表示する
5. `isProcessing`を`useState`で持ち、処理中はボタンの`disabled`に反映する
6. 実際にサンプルzip(CSVを含む)を作って動作確認(正しいzip、CSVが無いzip、列がズレているCSVなど)し、PRを出す

---

### パターンB: 変更後のやり方(機能単位で1チケットに統合 = 縦切り)

#### チケットB:「zip化CSVをアップロードして仮想ポイントを集計し、モーダルで結果を表示できるようにする」

**受け入れ条件(Acceptance Criteria)**
- `/upload`でzipファイルを選択してアップロードできる
- アップロードすると、zip内のCSVを解凍・パースし、C列(仮想ポイント)の合計と行数(対象ユーザー数)が正しく集計され、モーダルに表示される
- 1行目はヘッダー行としてスキップされる
- zip内にCSVが無い、パースに失敗する等の場合はエラーメッセージが表示される
- 処理中はボタンが無効化され、連打しても二重に処理が走らない

**完了条件(Definition of Done、プロジェクト共通)**
[definition-of-done.md](definition-of-done.md) のプロジェクト共通DoDに準拠(PRレビュー承認、検証環境での動作確認など)。

**作業手順(コードレベル)**
1. `jszip`・`papaparse`(および`@types/papaparse`)を`package.json`に追加し、`npm install`する
2. `upload.html`・`src/pages/upload/main.tsx`を作成し、`vite.config.ts`にビルドエントリと`/upload`のクリーンURLリライトを追加する
3. `src/utils/parseZipCsv.ts`で、zip解凍(`jszip`)→CSVパース(`papaparse`、ヘッダー行スキップ)→C列集計までの`aggregatePointsFromZip`関数を実装する(zip内にCSVが無い場合は`throw`する)
4. `src/components/Modal.tsx`で汎用モーダル(`isOpen` / `onClose` / `children`)を実装する
5. `src/pages/upload/App.tsx`で、
   - 選択ファイル・処理中フラグ・エラー・集計結果を`useState`で保持
   - アップロードボタン押下で`aggregatePointsFromZip`を呼び、成功したら結果をstateにセットしてモーダルを開く
   - 失敗したら`catch`でエラーメッセージをセットして表示する
6. [src/pages/list/App.tsx](front-end/src/pages/list/App.tsx)から`/upload`へのリンクを追加する
7. サンプルzip(正常系・CSVなし・列ズレなど)で動作確認し、PRを出す

**このパターンの利点**
- チケットが完了した時点で「実際の集計結果が正しいか」まで含めて確認できる
- ダミーUIを経由しない分、A/Bパターンで手順を比べると総作業量自体はほぼ変わらないが、途中成果物(ダミー表示のUI)がレビュー対象にならない分、レビュー回数・PR数が減る

### この題材ならではの注意点

コメント機能の例と違い、この機能は「UIだけ先に見せる」ことの価値がそもそも薄い(集計ロジックが本体で、UIはほぼ入力欄+ボタン+モーダルのみ)。**ロジックの比重が大きい機能ほど、工程で分割するメリットは小さくなる**ため、パターンBを選ぶ判断がより明確になる例として使えます。

